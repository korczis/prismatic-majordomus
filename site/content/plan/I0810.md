+++
title = "I0810 — A content-hash manifest that makes a second sync cheap"
description = "Implement the manifest: one row per source with its path, content hash, extractor version and the node ids it produced, written beside the generated graph, compared on every sync, and used so that a sync with no source change does no extraction and says so."
weight = 69
[extra]
plan_id = "I0810"
source = ".ai/repo/project/issues/I0810.yaml"
+++
