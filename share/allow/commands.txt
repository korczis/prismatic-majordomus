# AUTHORED FILE — no generator derives it yet; the keys of share/commands.yaml as the
# command registry (share/commands.yaml) declares them, read by test/cases/30_command_registry.sh.
# The gap and the plan: docs/TWO_FOLDER_CONSOLIDATION.md. A generated allow-list starts with
# "# GENERATED FILE"; this one does not, on purpose, so that nothing mistakes it for one.
^version$
^stages\.[0-9]+\.(id|title)$
^commands\.[0-9]+\.(id|title|summary|category|stage|visibility|class|requires_repository|requires_installed|requires_task|json|syntax|exit_codes|demo|note)$
^commands\.[0-9]+\.(reads|writes|exit_codes)(\.[0-9]+)?$
