# Objective
refuse tabs in the parser
# Current State
tabs are refused; the stray note under docs is reverted; the parser's test still fails
# Next Action
make the message name the column, then run the parser's test again
