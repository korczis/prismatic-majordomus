# liveness-check baseline — path:class of the debt that existed when the gate landed.
# A line here is known debt, not permission. Remove one by fixing the file; the gate
# reports a line that no longer matches anything so the list cannot rot.
scripts/cockpit-probe:unbounded-network
scripts/site-probe:unbounded-network
site/static/install.sh:unbounded-network
test/cases/100_execution_plane.sh:unbounded-network
test/cases/89_web_surface.sh:unbounded-network
