# Objective

Choose between transferring an unresolved question with a handover and widening the blocker
gate, and record the choice with what was rejected and why.

# Current State

Chosen: a question blocks a completed finish anywhere in its branch, not only in the task
that opened it. Both alternatives are recorded as rejected with the case each gets wrong,
and two conditions the choice imposes on the implementation are recorded with them — the
gate must skip the template comment block every fresh install ships, and `question resolve`
must reach a question the active task did not open.

# Next Action

Execute I0103: widen the gate in lib/check.sh, widen resolution in lib/question.sh, and
turn test/cases/50_blocker_across_handover.sh from a characterisation of the gap into a
case that fails when the behaviour is removed.
