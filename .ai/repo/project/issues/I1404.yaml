id: I1404
milestone: work-graph-github
title: The drift check is a gate, so a projection that stops being applied cannot stay silent
slug: projection-drift-gated
priority: p0
profile: implementation
parallel_safe: false
objective: "Run `scripts/github-sync --check` from CI on every change that can move the canonical model or the adapter, and fail the build on drift with the record and the remedy named."
why: "This is the root cause of the milestone. The adapter has exited 11 on drift since it was written and nothing has ever called it, so the projection decayed from complete to 10 per cent without one red build. `core-check` runs --plan and --render, which prove the adapter runs, not that the projection is current."
current_state: "scripts/ci/core-check lines 67 and 69 run --plan and --render. .ai/repo/ci/gates.yaml line 178 already lists scripts/github-sync in the paths that trigger the core gate, so the trigger exists and the check does not."
desired_state: "A drift finding is a build failure. The gate needs a token to read the remote, so it declares what it needs and says so when it is absent rather than passing quietly."
scope:
  - scripts/ci
  - .ai/repo/ci/gates.yaml
  - .github/workflows
  - .ai/repo/rules/project
  - test/cases
non_scope:
  - Applying the projection from CI, which stays a deliberate human act
depends_on:
  - I1402
acceptance_criteria:
  - The gate runs the drift check on every change to the model or the adapter, refuses behind, edited, conflict, unmanaged, state and milestone outright, and ratchets missing and adopt against a committed baseline
  - A missing or unauthorised token is reported as an unusable gate, never as a passing one
  - A rule states that the projection is gated, and doctor verifies the gate is wired rather than merely present
  - Introducing a canonical issue without projecting it turns the gate red, proved by a case
  - Moving a canonical record that was already projected turns the gate red, proved by a case
  - The baseline may fall and may never rise, and the gate refuses to run without one
validation:
  - scripts/ci/core-check
  - majordomus doctrine
  - bash test/run.sh
evidence_required:
  - drift_gated
risk: "Wiring a zero-tolerance gate before the backfill would make master red on a backlog nobody introduced today. The two tolerances are the mitigation: the states that mean new drift are refused from the first run, and the backlog is ratcheted against a committed baseline that may only fall. That inverts the dependency — the gate no longer waits for I1403, and I1403 becomes the act that lowers the baseline to zero."
created_at: 2026-09-09
updated_at: 2026-09-09T17:37:41Z
started_at: 2026-09-09T17:37:02Z
verified_at: 2026-09-09T17:37:27Z
completed_at: 2026-09-09T17:37:41Z
evidence:
  - covers: drift_gated
    type: test
    command: "bash test/run.sh 97_github_gate"
    result: "the gate refuses a record whose canonical text moved, breaks the ratchet on a record added to the model and never projected, refuses an edited region and an unmanaged remote issue, passes at the baseline, and refuses to run with no baseline; all proved offline against a fixture remote"
    commit: 21f7ad848b073ca25871758290a0756277b9aeb7
    recorded_at: 2026-09-09T17:37:13Z
  - covers: drift_gated
    type: ci
    command: "scripts/ci/github-check"
    result: "green on this tree at the committed baseline (missing 181, adopt 19); wired as gate github-projection in .ai/repo/ci/gates.yaml, selected by the layer and shell path classes, with issues:read and GH_TOKEN added to the structure job"
    commit: 21f7ad848b073ca25871758290a0756277b9aeb7
    recorded_at: 2026-09-09T17:37:21Z
