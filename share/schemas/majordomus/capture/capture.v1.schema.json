{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://majordomus.dev/schemas/majordomus/capture/capture.v1.schema.json",
  "x-majordomus-schema": "majordomus.capture/v1",
  "x-majordomus-derived-from": "majordomus/capture/capture.v1.proto",
  "description": "PromptDocument is `.ai/local/prompts/<YYYYMMDDHHMMSS>-<slug>.md`, beside the record of the same stem. It is a projection: it holds nothing the record does not, it is regenerated by `majordomus capture render`, and it is never edited by hand — an edit is lost on the next render, which is why the record and not this file is what the archive guarantees.  The field numbers are the order in the file:  ---                            1  front_matter --- # Prompt — <started_at>        2  heading | | |                          3  summary ## CONTEXT                     4  context      (optional) ## OUTPUT                      5  output       (optional) ## PROMPT                      6  prompt       (always, always last) The rendering as a whole: the front matter over the body.",
  "title": "capture",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "header",
    "body"
  ],
  "properties": {
    "header": {
      "$ref": "#/$defs/Header"
    },
    "body": {
      "$ref": "#/$defs/Body"
    }
  },
  "$defs": {
    "Header": {
      "description": "The front matter of a rendering: every member of the record except `text`, in the record's own declared order, then `record`. A member the record does not carry is omitted rather than written as null — absent says \"not observed\", null would claim it was observed to be nothing. The record is what these values came from; this is a projection of it and never the other way round.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "schema",
        "started_at",
        "provider",
        "event",
        "id",
        "record"
      ],
      "properties": {
        "schema": {
          "const": "majordomus.capture/v1"
        },
        "started_at": {
          "type": "string"
        },
        "provider": {
          "type": "string"
        },
        "event": {
          "type": "string"
        },
        "id": {
          "type": "string"
        },
        "session": {
          "type": "string"
        },
        "episode": {
          "description": "The record's identity, and the reason this schema has a second version's worth of fields at all. `session` is the provider's own session UUID, and turning it into the canonical episode id used to need a file the close of the episode deletes — so a closed episode's prompts became unattributable the moment it closed. `episode` is written at capture, while the mapping is still there.  `episode_link` says how: `open` (resolved against the open episode), `session-context` or `ledger` (linked afterwards from evidence that survived), `orphan` (no episode was open under that provider session) or `unlinked-legacy` (written before the field existed, and nothing survives to link it). The last two carry no episode. A reader that cannot tell an observation from an inference cannot tell what the archive proves, which is why the provenance is a field and not a convention.",
          "type": "string"
        },
        "episode_link": {
          "type": "string"
        },
        "repository_id": {
          "type": "string"
        },
        "worktree_id": {
          "type": "string"
        },
        "source": {
          "type": "string"
        },
        "cwd": {
          "type": "string"
        },
        "repository": {
          "type": "string"
        },
        "branch": {
          "type": "string"
        },
        "head": {
          "type": "string"
        },
        "finished_at": {
          "description": "Observable only after the turn the capture hook runs in front of, so absent from a record the UserPromptSubmit hook wrote rather than present and empty.",
          "type": "string"
        },
        "duration_ms": {
          "type": "integer"
        },
        "model": {
          "type": "string"
        },
        "effort": {
          "type": "string"
        },
        "tokens": {
          "type": "integer"
        },
        "meta": {
          "type": "string"
        },
        "record": {
          "description": "The file name of the JSON this was rendered from — a name, never a path, because a rendering can only ever point inside its own directory.",
          "type": "string"
        },
        "redacted": {
          "description": "Present only when the writer replaced credential material on its way to the record: the names of the shapes it matched, comma-separated. Absent means nothing was found, which is not the same claim as \"nothing was there\", and is why this is a field rather than a silence.",
          "type": "string"
        },
        "pruned_at": {
          "description": "The tombstone retention leaves. A prompt body over the declared age or size bound is replaced; the record, its identity and its provenance are not. These three say when the body went, how long it was and the digest of the raw span it was — enough to recognise a prompt somebody still holds a copy of, and not enough to reconstruct one nobody does.",
          "type": "string"
        },
        "pruned_bytes": {
          "type": "integer"
        },
        "pruned_sha256": {
          "type": "string"
        }
      }
    },
    "Section": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "heading": {
          "type": "string"
        },
        "level": {
          "type": "integer"
        },
        "empty": {
          "type": "boolean"
        }
      }
    },
    "Body": {
      "description": "The body: a heading, the same fields again as a table a person reads, then the prompt last and always. CONTEXT and OUTPUT are written when the record carries them.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "sections"
      ],
      "properties": {
        "sections": {
          "description": "Every heading of the body, in document order, as the reader flattens it.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Section"
          }
        }
      },
      "x-majordomus-sections": [
        {
          "heading": "PROMPT",
          "level": 2,
          "required": true,
          "non_empty": true
        }
      ]
    }
  },
  "x-majordomus-generated": "GENERATED FILE — DO NOT EDIT DIRECTLY; source: the document schema `share/schemas/majordomus/capture/capture.v1.proto`; regenerate with `majordomus generate`",
  "x-majordomus-generator": "majordomus-cli 0.6.1"
}
