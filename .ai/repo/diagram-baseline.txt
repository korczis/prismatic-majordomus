# Hand-drawn diagrams still in the published documents, as a count. Written by
# `scripts/ci/diagram-check --update`; the gate refuses a rise, and refuses a
# baseline above the truth. Drive it to zero by converting them to ```mermaid.
diagrams=2
