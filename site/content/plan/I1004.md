+++
title = "I1004 — Availability and visibility are metadata, not conditions in a template"
description = "Model where a capability is available — always, only in a static build, only with a running backend, only when authenticated, only at build time — and whether it is public or internal, so that every surface derives its behaviour from that rather than from its own conditionals."
weight = 140
[extra]
plan_id = "I1004"
source = ".ai/repo/project/issues/I1004.yaml"
+++
