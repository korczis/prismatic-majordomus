+++
title = "I0812 — Validate the graph, and let doctor and watch ask the same validator"
description = "Implement `knowledge validate` detecting duplicate node ids, dangling explicit references, source paths that no longer exist, hash mismatches, edges with a missing end, edges without provenance and a stale generated index, each with a severity and a reproduce command, and declare the rule in the doctrine registry so doctor and watch dispatch the same validator rather than reimplementing it."
weight = 71
[extra]
plan_id = "I0812"
source = ".ai/repo/project/issues/I0812.yaml"
+++
