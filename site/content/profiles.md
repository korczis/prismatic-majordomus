+++
title = "Execution profiles"
description = "Capability class, reasoning effort, output verbosity, presentation, context toggles and verification are independent fields. Rendered from the profile files a fresh install writes."
template = "profiles.html"
+++
A profile fixes what a task class needs on five axes that are never collapsed into one "quality" setting. Capability classes are not vendor model names; the generated instructions tell the worker to pick the closest its environment offers. Add a profile by adding a file; this page will include it.
