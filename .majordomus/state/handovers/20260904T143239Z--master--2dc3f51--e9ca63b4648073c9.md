---
schema_version: 1
created_at: 2026-09-04T14:32:39Z
task_id: t-20260904135842-729a
profile: deep-work
owner: "korczis"
repository_id: /Users/korczis/dev/prismatic-majordomus/.git
worktree: /Users/korczis/dev/prismatic-majordomus
branch: master
head: 2dc3f514ab870dc5b6760a8dbef8b348c7df3c25
working_tree: dirty
changed_files:
  - .majordomus/state/current.yaml
  - .majordomus/state/ledger.jsonl
  - .majordomus/state/archive/t-20260904070530-7e0c.yaml
  - .majordomus/state/checkpoints/20260904T143227Z--master--2dc3f51--57571aec13669d1e.md
---

# Objective
Make everything the /plan/ page renders derived from data: give the forward roadmap
milestones their issues, take the stale counts out of evidence prose, and replace the
status vocabulary, the milestone counts and the worker command sequence that are written
by hand in the templates and the generator with values read from the model.

# Current State
Committed and unpushed: the twenty-four issue contracts with their milestone scopes
(b3b106e), and the evidence prose rewrite (2dc3f51). plan validate reports no failures
and no warnings; generate-site-data is in sync; doctor reports no failures. The full
behavioural suite has not run against these commits — six concurrent runs on this machine
starved it and it was stopped rather than left competing.

Still hand-written and still to derive: the five status tiles in plan-section.html,
milestone.html and the milestone cards, which name done/ready/active/verify/blocked in
the template rather than reading the engine's status vocabulary; the "N of M done"
denominator, which uses total where the engine decides status on total minus cancelled;
the worker command sequence at the foot of /plan/, which duplicates the table in
share/skeleton/providers/body.md; and lifecycle.json's steps and divergence_labels,
which are JSON literals inside scripts/generate-site-data rather than read from
body.md and lib/common.sh.

This task's base commit went stale when master was merged, so peer-touched files are
reported outside its scope and the pre-push hook refuses. Nothing here is wrong; the
task simply cannot close from this base.

# Next Action
Start a task at the current commit scoped to lib/project.awk, lib/project.sh,
lib/plan.sh, scripts/generate-site-data, site/templates and test/cases, push the two
commits, then make the status vocabulary a declared list in the engine that every
surface reads.
