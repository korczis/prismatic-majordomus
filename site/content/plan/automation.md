+++
title = "automation — Repository automation is scripted capabilities over typed Rust, not shell"
description = "A capability may be scripted (ADR 0069): a Rhai script inside the Rust executable orchestrates typed capabilities through the one registry, reaches every surface by the projections that already exist, runs in a sandbox with a declared and enforced effect ceiling, and shell shrinks by migration under a gate that refuses new shell without a declared exemption."
weight = 21
template = "milestone.html"
[extra]
plan_id = "automation"
source = ".ai/repo/project/milestones/automation.yaml"
+++
