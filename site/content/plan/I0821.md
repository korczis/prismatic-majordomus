+++
title = "I0821 — Flattened YAML is cached under .ai/local/cache by content hash"
description = "Cache the output of mj_yaml_flatten under .ai/local/cache/flat/<sha256>, keyed by the file's content hash and the flattener's own version, so a second command on unchanged files does no parsing."
weight = 80
[extra]
plan_id = "I0821"
source = ".ai/repo/project/issues/I0821.yaml"
+++
