+++
title = "majordomus release version"
description = "The version the crate manifest declares, whether the projection the shell tool reads states it, any version written by hand where the tool's files live, and the bump the commits since the last release imply"
weight = 95
[extra]
route = "/docs/cli/release/version/"
command = "majordomus release version"
source = "apps/majordomus-cli/src/cli.rs"
+++
