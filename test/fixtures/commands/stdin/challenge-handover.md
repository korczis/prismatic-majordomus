# Objective
refuse tabs in the parser
# Current State
tabs are refused; the stray note under docs is reverted; test/parse.sh still fails
# Next Action
make the message name the column, then run sh test/parse.sh
