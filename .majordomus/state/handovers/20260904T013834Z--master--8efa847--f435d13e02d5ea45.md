---
schema_version: 1
created_at: 2026-09-04T01:38:34Z
task_id: t-20260904013330-780d
profile: debugging
owner: "korczis"
repository_id: /Users/korczis/dev/prismatic-majordomus/.git
worktree: /Users/korczis/dev/prismatic-majordomus
branch: master
head: 8efa847999e66bebe71b1c9b701e9fcc811451dd
working_tree: clean
changed_files:
---

# Objective

Record which checkout a task belongs to, so that a record on a shared branch stops
exporting its scope to every other reader.

# Current State

Done and committed as `fix(state): a task record names its checkout, and does not export
its scope`. `start` computes `worktree`; `check`, `finish --check` and `watch` report a
foreign record and enforce nothing; `finish` refuses to write to one; `start` proceeds past
it with a warning. Verified in a clean clone at a foreign path — the shape CI uses — where
`watch` now exits 0 and `doctor` exits 0 with hooks configured. Both mutations turn
27_foreign_task red. The suite is 26 passed, 0 failed.

This record is closed for the second occurrence of the other, unfixed limitation: another
session committed to this branch in this checkout while the task was open, so its files are
counted as this task's work and reported outside scope. That is a different problem from
the one just fixed, and it is documented rather than worked around.

# Next Action

Start the final task at the current head and produce the completion report.
