{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://majordomus.dev/schemas/majordomus/policy/policy.v1.schema.json",
  "x-majordomus-schema": "majordomus.policy/v1",
  "title": "Policy",
  "description": "The one provider-neutral policy, .ai/repo/policy.yaml: budgets, verification, retention, enforcement and projections.",
  "x-majordomus-allow": "policy",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "version"
  ],
  "properties": {
    "version": {
      "const": 1
    },
    "context": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "always_loaded_budget_lines": {
          "type": "integer",
          "minimum": 1
        },
        "builder_budget_lines": {
          "type": "integer",
          "minimum": 1
        },
        "recent_decisions": {
          "type": "integer",
          "minimum": 0
        },
        "max_list_items": {
          "type": "integer",
          "minimum": 1
        },
        "strategy": {
          "type": "string"
        },
        "transcript_is_state": {
          "type": "boolean"
        }
      }
    },
    "profiles": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "default": {
          "type": "string"
        },
        "checkpoint_interval_default": {
          "type": "string"
        }
      }
    },
    "verification": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "required_for_changes": {
          "type": "boolean"
        },
        "finish_requires": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "checkpoint": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "max_body_lines": {
          "type": "integer",
          "minimum": 1
        },
        "retention_max_files": {
          "type": "integer",
          "minimum": 1
        }
      }
    },
    "handover": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "required_sections": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "retention_max_files": {
          "type": "integer",
          "minimum": 1
        }
      }
    },
    "session": {
      "type": "object",
      "description": "What the provider's episode events do beyond drawing the boundary: how much the start event hands the worker, and whether the compaction and end events record anything.",
      "additionalProperties": false,
      "properties": {
        "briefing_on_start": {
          "type": "boolean",
          "description": "Hand the worker a briefing on the start event's standard output, which the provider adds to the context it is about to build. False restores the older silence, in which a continuation record is written and nothing loads it."
        },
        "briefing_budget_lines": {
          "type": "integer",
          "minimum": 1,
          "description": "Hard cap on what the start event writes to the provider's context. Text that arrives in every episode is paid for in every episode."
        },
        "checkpoint_on_compact": {
          "type": "boolean",
          "description": "Record a derived checkpoint when the provider announces a compaction, and again when an episode closes. Both are moments at which what the episode knows stops being reachable. Neither asks whether a task is open: a checkpoint is the episode's record and a task is an optional relation it names when it has one (ADR 0041)."
        },
        "handover_on_end": {
          "type": "boolean",
          "description": "Write a derived continuation record when an episode ends. Until ADR 0041 this happened only while a task was still active, so a repository whose last task had been handed over stopped writing the one record a future worker resumes from."
        },
        "ensure_server_on_start": {
          "type": "boolean",
          "description": "On the start event, make sure the repository's shared server serves this checkout (`majordomus serve ensure`): a server is started when none answers, never built, and the briefing names where it stands. False leaves the server to the first MCP client."
        },
        "freshness": {
          "type": "object",
          "description": "How old a continuation record may be before it stops being current. This is a different question from divergence (exact/advanced/diverged/different_context), which says where the record's commit sits relative to HEAD and nothing whatever about time: a record is `advanced` on the day it is written and still `advanced` a month later. Declared once here and read by every surface; a second copy of either number in a shell function, a Rust constant or a page of prose is the drift this file exists to prevent.",
          "additionalProperties": false,
          "required": [
            "fresh_minutes",
            "stale_minutes"
          ],
          "properties": {
            "fresh_minutes": {
              "type": "integer",
              "minimum": 1,
              "description": "Below this age a record is `fresh`: current, act on it."
            },
            "stale_minutes": {
              "type": "integer",
              "minimum": 1,
              "description": "At or beyond this age a record is `stale`: history, never presented as the current instruction. Between the two it is `aging` \u2014 still current, but its age is stated."
            }
          }
        }
      }
    },
    "ledger": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "retention_max_lines": {
          "type": "integer",
          "minimum": 1
        }
      }
    },
    "enforcement": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "name",
          "path",
          "wired_by"
        ],
        "properties": {
          "name": {
            "type": "string"
          },
          "path": {
            "type": "string"
          },
          "wired_by": {
            "type": "string"
          },
          "args": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      }
    },
    "projections": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "provider",
          "target"
        ],
        "properties": {
          "provider": {
            "type": "string"
          },
          "target": {
            "type": "string"
          },
          "always_loaded": {
            "type": "boolean"
          },
          "mode": {
            "type": "string"
          }
        }
      }
    },
    "benchmark": {
      "type": "object",
      "additionalProperties": false,
      "description": "How majordomus bench samples, and the thresholds bench --check and the command budgets apply.",
      "properties": {
        "samples": {
          "type": "integer",
          "minimum": 1
        },
        "warmup": {
          "type": "integer",
          "minimum": 0
        },
        "regression": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "p50": {
              "type": "number",
              "minimum": 0
            },
            "p95": {
              "type": "number",
              "minimum": 0
            },
            "p99": {
              "type": "number",
              "minimum": 0
            }
          }
        },
        "budget": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "doctor_ms": {
              "type": "integer",
              "minimum": 1
            },
            "watch_ms": {
              "type": "integer",
              "minimum": 1
            }
          }
        }
      }
    },
    "use_cases": {
      "type": "object",
      "additionalProperties": false,
      "description": "How use-case coverage is gated: per class of target, whether a gap fails, is reported, or is not looked at.",
      "properties": {
        "coverage": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "commands": {
              "enum": [
                "required",
                "advisory",
                "off"
              ]
            },
            "claims": {
              "enum": [
                "required",
                "advisory",
                "off"
              ]
            },
            "mcp_tools": {
              "enum": [
                "required",
                "advisory",
                "off"
              ]
            }
          }
        }
      }
    }
  }
}
