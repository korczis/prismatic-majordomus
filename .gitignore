# editor and OS noise
.DS_Store
*.swp
*~

# test scratch
/test/tmp/

# site build dependencies (not part of the CLI)
node_modules/

# derived GitHub Pages site. All three are rewritten on every scripts/site-build;
# editing one loses the edit and hides a missing canonical source.
public/
site/data/generated/
site/content/
