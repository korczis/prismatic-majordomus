+++
title = "I0805 — Read sessions back with a divergence label"
description = "Implement `session list`, `session show <id>` and `session latest`, each resolving with the established rule — same repository, same worktree, same branch, then same branch — and each printing the existing divergence label of the record against current git."
weight = 64
[extra]
plan_id = "I0805"
source = ".ai/repo/project/issues/I0805.yaml"
+++
