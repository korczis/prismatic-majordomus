# Objective

Make every non-trivial change in this repository belong to a milestone specification and
one dependency-linked issue execution contract, validated as a graph, with status derived
rather than asserted, and projected to GitHub and to the website from one canonical model.

# Current State

M000 is DONE: fifteen issues complete and its four acceptance tokens covered. The canonical
model lives in .majordomus/project, lib/project.awk derives every status, wave and finding,
and the CLI, the GitHub adapter and the site generator all read that one engine. GitHub
carries eight milestones and nineteen issues with no drift. M001 is ACTIVE at one of four.

The full behavioural suite passed 41 of 41 at the tree this task produced, and that run is
recorded as M000's `suite` acceptance evidence. This task's own verify command is the
integration gate set rather than a second run of the same suite, because the machine is
running six concurrent suites from other sessions and a duplicate run would record nothing
the recorded one does not already say.

# Next Action

Execute I0102 — decide whether an unresolved question transfers with a handover or the
blocker gate widens — and record the decision with what was rejected and why.
