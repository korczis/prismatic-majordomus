+++
title = "I1704 — A repository-writing capability is marked destructive on every surface, and the exposure policy for writes is one decision"
description = "Make every capability whose effect is a repository mutation carry that effect identically on each surface it is projected to — the MCP destructive and read-only annotations, the OpenAPI operation, the command line's reference — and record, as one decision the owner accepts, whether a capability that writes the repository may be exposed over MCP and HTTP at all."
weight = 242
[extra]
plan_id = "I1704"
source = ".ai/repo/project/issues/I1704.yaml"
+++
