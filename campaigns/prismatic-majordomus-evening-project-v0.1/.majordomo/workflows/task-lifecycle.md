# Task Lifecycle

States: `new → active → verification → completed`, with `blocked` and `handed_over` when appropriate.

Before work define objective, scope, out-of-scope, profile, dependencies, and acceptance criteria. During execution checkpoint meaningful changes and reassess the plan. Complete only when required acceptance criteria and verification are satisfied.
