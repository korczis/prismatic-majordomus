USAGE

Recommended working directory:
  ~/dev/prismatic-majordomus

Recommended extraction:
  mkdir -p ~/dev/prismatic-majordomus/tmp/majordomus-openai-provider-control-plane-prompts
  unzip majordomus-openai-provider-control-plane-prompts.zip -d ~/dev/prismatic-majordomus/tmp/majordomus-openai-provider-control-plane-prompts

Then execute prompts in numeric order using a high-context coding agent.

IMPORTANT:
- Let every prompt re-read the live repository.
- Do not paste all prompts into one session.
- Prefer a fresh high-context session per phase with Majordomus handover/session context enabled.
- After each phase, require committed, tested work and a clean handover before moving to the next.
- If a prompt discovers that a later phase already exists in the repository, it should validate/reuse it rather than reimplementing it.
- If live rules conflict with this pack, the live repository wins.
- If OpenAI APIs changed since this pack was prepared, current official OpenAI documentation wins.
- Do not use undocumented ChatGPT web APIs as architecture.
