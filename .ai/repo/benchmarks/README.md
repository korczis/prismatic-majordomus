---
schema: context/v1
id: ai.repo.benchmarks
kind: context
title: Benchmarks
description: The recorded baselines a performance claim is compared against, one directory per measured surface.
status: active
scope: subtree
providers: ["*"]
audience: [human, agent]
composition: extend
order: 100
---

# Benchmarks

A performance claim in this repository names a measurement, and the measurement is
compared against a recorded baseline that lives here. One directory per measured surface:
`rust/` for the executable's own commands, `pages/` for the published site. Nothing here
is prose about speed; the prose is `docs/PERFORMANCE.md`, and it cites these files rather
than restating their numbers.

A baseline is data a run produced on a named platform, kept so that the next run has
something to disagree with. It is regenerated by the command that measures it, never
edited by hand to make a gate pass: a number nobody measured is the failure the whole
arrangement exists to prevent.
