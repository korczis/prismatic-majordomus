---
schema_version: 1
created_at: 2026-09-04T00:59:04Z
task_id: t-20260903235710-5e89
profile: deep-work
owner: "korczis"
repository_id: /Users/korczis/dev/prismatic-majordomus/.git
worktree: /Users/korczis/dev/prismatic-majordomus
branch: master
head: 359b45e2d16cfe9d29daecb6822090d1405a9b86
working_tree: clean
changed_files:
---

# Objective

Add a repository-local working-memory and session-continuity subsystem: context assembly,
checkpoints, an operational history, decision and open-question stores, prompt assets, and
literal retrieval, wired into the existing lifecycle and enforced by CI.

# Current State

Implemented, tested and committed in three commits on master (`feat(memory)`,
`test(memory)`, `test: renumber`), plus one reconciling the command-surface case with a
concurrent branch. Seven commands exist and are dispatched: context, checkpoint, history,
decision, question, prompt, search. `bash test/run.sh` is 24 passed, 0 failed; shellcheck
is clean over bin, lib, the harness and every case; doctor reports 0 failures.

This task record is being closed for a mechanical reason, not because the objective is
done: two other sessions committed to master while it was open, so the base head recorded
at start is behind their work and every file they touched is reported outside my scope.
The scope check is right; the record is simply stale.

# Next Action

Start a fresh task at the current head with the same scope and continue with the
documentation, the claims matrix, and the provider projection, which are the parts of the
objective not yet done.

# Completed

- context, checkpoint, history, decision, question, prompt and search, with their policy
  keys, allowlists, skeleton prompt assets and the layout migration in `update`.
- Lifecycle integration: start resolves a prior record, check validates the stores and
  names the newest checkpoint, finish records the checkpoint count and asks a partial or
  blocked task for a continuation record, doctor proves each store is reachable through
  its own command, watch reports continuity drift.
- Seven test cases, ten mutation checks, and CI wiring including a meta-case that proves
  the workflows call the canonical command and cannot go green on a failure.

# Decisions

Recorded in `.majordomus/state/decisions.md` for this task.

# Verification

`bash test/run.sh` — 24 passed, 0 failed.
`shellcheck -x -s bash -S warning bin/majordomus lib/*.sh test/run.sh test/lib.sh test/cases/*.sh` — exit 0.
`bin/majordomus doctor` — 0 failures. `bin/majordomus watch` — 0 drift.

# Risks

A task's base head goes stale when another session commits to the same branch in the same
checkout, and every file that session touched is then reported outside the task's scope.
Majordomus cannot distinguish authorship within a shared branch; this is a real limitation
and belongs in the documentation rather than being worked around.

# Open Work

Documentation (a continuity concept page, the schemas of the new records, the concepts
table), the claims matrix with a detail page per new claim, and the provider projection
that teaches a worker the workflow.
