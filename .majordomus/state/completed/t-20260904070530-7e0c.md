# Objective

Implement the decision M001/I0102 recorded — a question refuses a completed finish anywhere
on its branch — and turn the characterisation case into one that fails when the behaviour is
removed.

# Current State

Done, and I0104 with it. The gate reads every unresolved entry, skips the comment block the
shipped template lives in, refuses only `completed`, and any task can resolve any question.
Case 50 asserts it and reproduces the escape from a probe that narrows the gate back; case 25
now asserts that an inherited blocker still blocks; case 17 answers its question before the
section about the advisory class, which the widened gate would otherwise mask. The claim is
guaranteed with its implementation and its test, and the doctrine summary matches the gate.

site/templates and scripts/lib appear in this task's diff range because a merge from the
peer session brought them in, not because this task edited them.

# Next Action

Record M001's three acceptance tokens and let the milestone derive DONE.
