# Published tags this tree holds no release record for: known debt, not permission.
# Read by scripts/ci/release-check (check 5). One tag per line, with the thing that
# clears it. The gate reports a line whose tag is now recorded, or that names no tag
# any more, so the list cannot rot — a cleared line is deleted by the change that
# clears it.
v0.1.0  # published before the layer held release records; only a retroactive record clears it
v0.2.0  # published before branch protection, when the record was pushed straight at master and the push was declined; only a retroactive record clears it
v0.3.0  # same wound as v0.2.0; only a retroactive record clears it
v0.7.0  # the record is in flight as PR #579 (branch release/record-v0.7.0); delete this line when it merges
