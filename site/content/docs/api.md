+++
title = "HTTP API"
description = "The projection of a repository's AI layer: every rule, prompt, session and project record under .ai/, the capabilities that read them and the declared commands that change them, served over MCP, HTTP and the command line from one registry."
weight = 39
template = "api.html"
[extra]
source = "docs/generated/openapi.json"
+++
