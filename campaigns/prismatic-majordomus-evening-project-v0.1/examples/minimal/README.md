# Minimal integration

Copy `.majordomo/`, add the short AGENTS/CLAUDE pointer, update `.majordomo/state/current.md`, select the smallest appropriate profile, then follow verification/handover rules.
