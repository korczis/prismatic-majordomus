+++
title = "I0201 — Declare which profile axis may become which runtime limit"
description = "Add one canonical file beside the profiles that maps a profile axis to the runtime limit it may derive, so that a limit has a single source, an axis nobody can enforce cannot be listed, and a limit no adapter applies is a validation error rather than a comment."
weight = 32
[extra]
plan_id = "I0201"
source = ".ai/repo/project/issues/I0201.yaml"
+++
