{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://majordomus.dev/schemas/majordomus/session-record/session-record.v1.schema.json",
  "title": "Session record",
  "description": "The front matter of a closed execution episode under .ai/repo/sessions/: what the repository can prove about a stretch of work \u2014 its identity and times, the branch and the heads it moved between, the commits it produced, the paths it changed, and the outcome. Written by `majordomus session close` from git and the ledger, never from a transcript: the optional body summarises the work, and this contract has no field for a conversation (ADR 0014, project.never-store-transcripts). The open episode's own state file is a different object with its own schema (session).",
  "x-majordomus-allow": "session-record",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema",
    "kind",
    "session_id",
    "started_at",
    "closed_at",
    "outcome"
  ],
  "properties": {
    "schema": {
      "description": "The format version of this record. A version the tool does not know is refused, never guessed.",
      "const": "session/v1"
    },
    "kind": {
      "const": "session"
    },
    "session_id": {
      "description": "The identity, allocated when the episode opened and never reused.",
      "type": "string",
      "minLength": 1
    },
    "started_at": {
      "description": "When the episode opened, UTC, RFC 3339.",
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$"
    },
    "closed_at": {
      "description": "When it closed, UTC, RFC 3339.",
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$"
    },
    "created_at": {
      "description": "When the record was written; the closing time.",
      "type": "string"
    },
    "outcome": {
      "description": "closed: the episode ended deliberately. interrupted: it stopped without one.",
      "enum": [
        "closed",
        "interrupted"
      ]
    },
    "title": {
      "description": "One line naming the work, for a listing.",
      "type": "string"
    },
    "task_id": {
      "description": "The task the episode belonged to, when it belonged to one.",
      "type": "string"
    },
    "profile": {
      "description": "The profile the task was worked under.",
      "type": "string"
    },
    "worker": {
      "description": "What did the work, as it identified itself: a tool and a model, never a person's name.",
      "type": "string"
    },
    "branch": {
      "description": "The branch the episode ran on.",
      "type": "string"
    },
    "start_head": {
      "description": "The commit the episode started from.",
      "type": "string"
    },
    "head": {
      "description": "The commit it ended at.",
      "type": "string"
    },
    "start_working_tree": {
      "description": "Whether the working tree was clean or dirty when the episode opened.",
      "enum": [
        "clean",
        "dirty"
      ]
    },
    "working_tree": {
      "description": "Whether it was clean or dirty when the episode closed.",
      "enum": [
        "clean",
        "dirty"
      ]
    },
    "commits": {
      "description": "The commits between the two heads, oldest first.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "changed_files": {
      "description": "Repository-relative paths the working tree held changed when the episode closed.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "repository_id": {
      "description": "The repository the episode ran in, as the tool identifies it.",
      "type": "string"
    },
    "schema_version": {
      "description": "Present on records written before the schema identifier; read, never written.",
      "type": "integer"
    },
    "worktree_id": {
      "description": "A stable identity for the working copy the episode ran in, naming no path: the resolver uses it to tell this checkout's records from another's.",
      "type": "string",
      "pattern": "^[0-9a-f]{8,64}$"
    },
    "tasks": {
      "description": "The tasks the episode worked on, by id. Derived from the ledger's own events for this episode; never authored.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "issues": {
      "description": "The issues it moved, by id. Derived from the ledger's own events for this episode; never authored.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "milestones": {
      "description": "The milestones those issues belong to, by id. Derived from the ledger's own events for this episode; never authored.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "checkpoints": {
      "description": "The checkpoints it recorded, by path. Derived from the ledger's own events for this episode; never authored.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "handovers": {
      "description": "The handover records it wrote, by path. Derived from the ledger's own events for this episode; never authored.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "decisions": {
      "description": "The decisions it recorded, by title. Derived from the ledger's own events for this episode; never authored.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "questions": {
      "description": "The questions it opened or resolved. Derived from the ledger's own events for this episode; never authored.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "evidence": {
      "description": "The evidence it attached, as the ledger recorded it. Derived from the ledger's own events for this episode; never authored.",
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  },
  "x-majordomus-schema": "majordomus.session-record/v1"
}
