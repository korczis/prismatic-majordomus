+++
title = "I0902 — The deployment object becomes typed Rust that refuses nonsense"
description = "Parse the canonical deployment object into strongly typed Rust — enums and newtypes rather than strings — and validate it so that a deployment that cannot work is refused before anything is built."
weight = 108
[extra]
plan_id = "I0902"
source = ".ai/repo/project/issues/I0902.yaml"
+++
