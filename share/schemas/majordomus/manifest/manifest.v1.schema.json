{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://majordomus.dev/schemas/manifest.schema.json",
  "title": "Manifest",
  "description": "The section registry of the repository's AI layer, .ai/manifest.yaml. Discovery reads it and never walks the directory.",
  "x-majordomus-allow": "manifest",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema",
    "repo",
    "local",
    "sections"
  ],
  "properties": {
    "schema": {
      "const": "ai-repository/v1"
    },
    "repo": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "path"
      ],
      "properties": {
        "path": {
          "type": "string"
        }
      }
    },
    "local": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "path",
        "tracked",
        "implicit_context"
      ],
      "properties": {
        "path": {
          "type": "string"
        },
        "tracked": {
          "type": "boolean"
        },
        "implicit_context": {
          "type": "boolean"
        }
      }
    },
    "sections": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "adrs": {
          "type": "string"
        },
        "applications": {
          "type": "string"
        },
        "deployments": {
          "type": "string",
          "description": "The deployments of this repository's executable, one canonical object each; every provider artifact is generated from one of them."
        },
        "features": {
          "type": "string",
          "description": "The product features of this repository, one canonical object each; every surface that presents a feature is derived from them (ADR 0022)."
        },
        "knowledge": {
          "type": "string"
        },
        "policy": {
          "type": "string"
        },
        "profiles": {
          "type": "string"
        },
        "project": {
          "type": "string"
        },
        "prompts": {
          "type": "string"
        },
        "releases": {
          "type": "string",
          "description": "The releases this repository published, one record each; the public release metadata is a projection of them."
        },
        "rules": {
          "type": "string"
        },
        "scope": {
          "type": "string"
        },
        "sessions": {
          "type": "string",
          "description": "The closed execution episodes, one record each (ADR 0014)."
        },
        "skills": {
          "type": "string"
        },
        "use-cases": {
          "type": "string"
        },
        "why": {
          "type": "string",
          "description": "The operational moments this tool exists for, the audiences that recognise them and the areas they fall under (ADR 0018)."
        },
        "workflows": {
          "type": "string"
        }
      }
    },
    "context": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "documents": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    }
  }
}
