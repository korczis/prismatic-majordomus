# Verification

Use the repository's real validation surface. Verify syntax/format, compilation, tests, static analysis, security checks, regression behavior, working-tree state, and documentation consistency as applicable. Never invent passing evidence. If something cannot be verified, state that explicitly.
