# Security

Majordomus v0.1 is local-first. No hidden telemetry, credential collection, implicit network calls, `eval`, generated-command execution, `curl | sh`, destructive recursive deletion, or silent state overwrite.
