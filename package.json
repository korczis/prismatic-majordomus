{
  "name": "prismatic-majordomus-site",
  "version": "0.1.0",
  "private": true,
  "description": "Build dependencies for the derived GitHub Pages site. Not part of the majordomus CLI, which is portable shell with no runtime dependencies.",
  "license": "MIT",
  "scripts": {
    "build": "scripts/generate-pages",
    "check": "scripts/generate-pages --check",
    "serve": "scripts/generate-pages --serve"
  },
  "devDependencies": {
    "@tailwindcss/cli": "4.1.14",
    "flowbite": "3.1.2",
    "flowbite-typography": "1.0.5",
    "tailwindcss": "4.1.14"
  }
}
