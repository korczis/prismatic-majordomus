---
schema_version: 1
created_at: 2026-09-04T17:18:17Z
task_id: t-20260904143326-9f63
profile: deep-work
owner: "korczis"
repository_id: /Users/korczis/dev/prismatic-majordomus/.git
worktree: /Users/korczis/dev/prismatic-majordomus
branch: master
head: d6aca744e1cc199dc34165e912ff5efa8fa6e093
working_tree: clean
changed_files:
---

# Objective

Make everything the /plan/ page renders derived from the model, so no number or
cross-link on the site is written by hand.

# Current State

Landed on master in four commits (20f9485, b34f250, 31965b1, and the regeneration
commit on top):

- Counts are rendered, never written. Six templates count the collection they
  describe (lifecycle.principles, policy.enforcement, why.moments); copy in
  site/content-src, site/data/marketing.toml and the RESPONSIBILITIES.yaml header
  no longer states a fixed number.
- Responsibility cross-links come from canonical records. docs/RESPONSIBILITIES.yaml
  joined the canonical input set; slug and command are read from it, and claim links
  come from the responsibility field each claim declares in docs/CLAIMS.yaml. The
  hand-maintained keyword-regex table in scripts/generate-site-data is gone, and the
  generator exits 10 on a RESPONSIBILITIES.yaml that is not version 1.
- scripts/generate-site-data --check exits 0, in sync at 232869e16cb4.
- doctor reports 0 failures.

This task is being closed for a mechanical reason, not because the objective is
finished: feature/session-knowledge was merged into master by another session in this
same checkout, so the task base 2dc3f51 is stale and .gitignore and bin/majordomus,
both touched only by that session's commits 0db0501 and 34cd249, are reported outside
the claimed scope. finish --check refuses the push on those two lines.

Nothing is pushed yet. origin/master is behind master.

# Next Action

Start a new task at the current commit with a scope that covers the same paths, then
push master. The push deploys Pages, and the peer session working on M003 announced it
intends to deploy as well, so coordinate before pushing rather than racing it.

Still open on the objective: doctor reports WARN dag I0804 shares test/cases with
I0808, so they may not run concurrently. That predates this work.
