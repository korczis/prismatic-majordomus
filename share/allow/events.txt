# AUTHORED FILE — no generator derives it yet; the keys of share/events.yaml as the event
# vocabulary (share/events.yaml) declares them, read by lib/common.sh when a ledger event is
# appended. The gap and the plan: docs/TWO_FOLDER_CONSOLIDATION.md. A generated allow-list
# starts with "# GENERATED FILE"; this one does not, on purpose.
^version$
^events\.[0-9]+\.(id|title|summary|emitted_by|public|note)$
^events\.[0-9]+\.requires(\.[0-9]+)?$
