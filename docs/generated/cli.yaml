# GENERATED FILE — DO NOT EDIT DIRECTLY
# Source: the clap declaration in apps/majordomus-cli/src/cli.rs and the examples beside it; regenerate with `majordomus generate`
# Generator: majordomus-cli 0.5.0
schema: majordomus/cli/v1
generated: "GENERATED FILE — DO NOT EDIT DIRECTLY; source: the clap declaration in apps/majordomus-cli/src/cli.rs and the examples beside it; regenerate with `majordomus generate`"
generator: majordomus-cli 0.5.0
version: "0.5.0"
source: apps/majordomus-cli/src/cli.rs
route_prefix: /docs/cli
cli:
  path:
    - majordomus
  route: /docs/cli/
  executable: false
  usage: "majordomus <COMMAND>"
  about: "Majordomus control plane: a data-driven MCP server over the repository's .ai/ layer"
  long_about: "The Rust executable of Majordomus. It reads the repository's provider-neutral AI layer under .ai/ and serves it, read-only, to MCP clients over stdio.\n\nThe task lifecycle (init, start, check, finish, doctor, ...) is the shell tool bin/majordomus in the same repository; this executable does not implement those commands."
  args: []
  examples: []
  subcommands:
    - path:
        - majordomus
        - mcp
      route: /docs/cli/mcp/
      executable: true
      usage: "majordomus mcp [OPTIONS]"
      about: "Serve the repository's AI layer to an MCP client over stdio (read-only)"
      args:
        - name: repo
          long: repo
          positional: false
          help: "Start the search for the repository root here (default: the current directory)"
          value_name: PATH
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: discovery
          long: discovery
          positional: false
          help: How declarative files are enumerated
          value_name: DISCOVERY
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: vcs
              help: "Tracked files, through the version-control index (the layer's contract)"
            - name: filesystem
              help: A walk of the work tree with the same glob semantics; untracked files included
          defaults:
            - vcs
        - name: strict
          long: strict
          positional: false
          help: Refuse to proceed when any file of the layer carries an error diagnostic
          value_name: STRICT
          takes_value: false
          required: false
          global: true
          possible_values: []
          defaults:
            - "false"
        - name: share
          long: share
          positional: false
          help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
          value_name: DIR
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: inspect
          long: inspect
          positional: false
          help: "Print what would be served, and every diagnostic, then exit without serving"
          value_name: INSPECT
          takes_value: false
          required: false
          global: false
          possible_values: []
          defaults:
            - "false"
        - name: format
          long: format
          positional: false
          help: Output shape of --inspect
          value_name: FORMAT
          takes_value: true
          required: false
          global: false
          possible_values:
            - name: text
              help: Lines for a person
            - name: json
              help: "One JSON document, deterministic"
          defaults:
            - text
        - name: transport
          long: transport
          positional: false
          help: The transport to serve on
          value_name: TRANSPORT
          takes_value: true
          required: false
          global: false
          possible_values:
            - name: stdio
              help: One JSON-RPC frame per line on stdin and stdout
          defaults:
            - stdio
        - name: standalone
          long: standalone
          positional: false
          help: "Serve this client alone: no shared server, no HTTP, no Swagger UI, no peers, and nothing written anywhere. The default is the shared server (below)"
          value_name: STANDALONE
          takes_value: false
          required: false
          global: false
          possible_values: []
          defaults:
            - "false"
        - name: http_host
          long: http-host
          positional: false
          help: Interface the shared server binds when this process is the one that starts it
          value_name: HOST
          takes_value: true
          required: false
          global: false
          possible_values: []
          defaults:
            - "127.0.0.1"
        - name: http_port
          long: http-port
          positional: false
          help: "Port the shared server binds when this process starts it; when it is taken, a free port is used instead and the URL is logged on stderr either way"
          value_name: PORT
          takes_value: true
          required: false
          global: false
          possible_values: []
          defaults:
            - "8741"
      examples:
        - id: mcp-inspect
          title: "See what would be served, without serving it"
          description: "Builds the registry and the index of the repository in the working directory and prints the repository, the capabilities, the objects and every diagnostic, then exits. Nothing is served and nothing is written."
          argv:
            - mcp
            - "--inspect"
          command: majordomus mcp --inspect
          setup: []
          expectation: "exits 0; prints repository, capabilities"
        - id: mcp-inspect-json
          title: "The same, as one JSON document for a script"
          description: "The shape `--inspect` prints for a person, as JSON: the repository, its discovery mode, the capabilities and the diagnostics, deterministic and safe to diff."
          argv:
            - mcp
            - "--inspect"
            - "--format"
            - json
          command: majordomus mcp --inspect --format json
          setup: []
          expectation: "exits 0; prints one JSON document carrying /repository/repository/root, /tools"
        - id: mcp-stdio
          title: Serve one MCP client on stdio
          description: "The form an MCP client spawns: JSON-RPC frames in on stdin, frames out on stdout, logs on stderr, and the session ends at end of input. `--standalone` keeps this process to itself: no shared server, no HTTP, nothing written anywhere."
          argv:
            - mcp
            - "--standalone"
          command: majordomus mcp --standalone
          setup: []
          expectation: "answers initialize and tools/list on stdio, and exits 0 at end of input"
      subcommands: []
    - path:
        - majordomus
        - serve
      route: /docs/cli/serve/
      executable: true
      usage: "majordomus serve [OPTIONS] [COMMAND]"
      about: "Serve the same capabilities over HTTP on the loopback interface, with the home page, /openapi.json, /swagger and the documentation under /docs/ (read-only)"
      args:
        - name: repo
          long: repo
          positional: false
          help: "Start the search for the repository root here (default: the current directory)"
          value_name: PATH
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: discovery
          long: discovery
          positional: false
          help: How declarative files are enumerated
          value_name: DISCOVERY
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: vcs
              help: "Tracked files, through the version-control index (the layer's contract)"
            - name: filesystem
              help: A walk of the work tree with the same glob semantics; untracked files included
          defaults:
            - vcs
        - name: strict
          long: strict
          positional: false
          help: Refuse to proceed when any file of the layer carries an error diagnostic
          value_name: STRICT
          takes_value: false
          required: false
          global: true
          possible_values: []
          defaults:
            - "false"
        - name: share
          long: share
          positional: false
          help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
          value_name: DIR
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: host
          long: host
          positional: false
          help: Interface to bind; loopback unless you say otherwise
          value_name: HOST
          takes_value: true
          required: false
          global: false
          possible_values: []
          defaults:
            - "127.0.0.1"
        - name: port
          long: port
          positional: false
          help: Port to bind; 0 picks a free one and the address is logged on stderr
          value_name: PORT
          takes_value: true
          required: false
          global: false
          possible_values: []
          defaults:
            - "8741"
        - name: fallback
          long: fallback
          positional: false
          help: "When the port is taken, bind a free one instead and log both; without this a taken port is an error"
          value_name: FALLBACK
          takes_value: false
          required: false
          global: false
          possible_values: []
          defaults:
            - "false"
        - name: idle
          long: idle
          positional: false
          help: Stop when no peer has been attached for this many seconds; 0 runs until stopped. What a server no client owns is started with
          value_name: SECONDS
          takes_value: true
          required: false
          global: false
          possible_values: []
          defaults:
            - "0"
        - name: deployment
          long: deployment
          positional: false
          help: "Bind the address this deployment object declares (`.ai/repo/deployments/<ID>.yaml`) instead of the local default. What a hosted process is started with; the address is the object's, not this command line's"
          value_name: ID
          takes_value: true
          required: false
          global: false
          possible_values: []
          defaults: []
      examples:
        - id: serve-ephemeral-port
          title: Serve the same capabilities over HTTP on a free port
          description: "Port 0 asks the operating system for a free port; the address is logged on stderr. `/` is the home page, generated from the surfaces this process resolved; the document at /openapi.json is the same one `majordomus generate` commits; /swagger is the Swagger UI over it; /docs/ is this repository's documentation when it has been built for that mount."
          argv:
            - serve
            - "--port"
            - "0"
          command: majordomus serve --port 0
          setup: []
          expectation: "binds a port, answers GET /openapi.json, exits 0 when stopped"
      subcommands:
        - path:
            - majordomus
            - serve
            - status
          route: /docs/cli/serve/status/
          executable: true
          usage: "majordomus serve status [OPTIONS]"
          about: "Where this checkout's server stands — absent, starting, ready, outdated or stale — and every server of the repository"
          args:
            - name: checkouts
              long: checkouts
              positional: false
              help: "Which checkouts to answer for: every checkout of the repository, or this one alone — which reads no other checkout's lease and probes no other server"
              value_name: CHECKOUTS
              takes_value: true
              required: false
              global: false
              possible_values:
                - name: repository
                  help: "Every checkout git registers for the repository, the primary first — a lease read and a probe each. The default, because it is the answer this capability gave before there was anything to ask"
                - name: this
                  help: "This checkout alone. No other checkout is enumerated, no other lease is read and no other server is probed; `servers` holds the one entry, and it is this one"
              defaults:
                - repository
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: false
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: serve-status
              title: "Where this checkout's server stands, and every server of the repository"
              description: "The standing of this checkout's server measured against what this executable would serve, the lease it holds, and every checkout git registers for the repository with its own server. Asked of the running server when there is one, so that the answer includes the lease that process holds; answered locally otherwise."
              argv:
                - serve
                - status
              command: majordomus serve status
              setup: []
              expectation: exits 0; prints standing
          subcommands: []
        - path:
            - majordomus
            - serve
            - ensure
          route: /docs/cli/serve/ensure/
          executable: true
          usage: "majordomus serve ensure [OPTIONS]"
          about: "Make sure a ready server serves this checkout: start one when there is none or the lease is stale, wait for one that is starting, and report where it stands"
          args:
            - name: port
              long: port
              positional: false
              help: The port the started server asks for first; a taken one is replaced by a free one
              value_name: PORT
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults:
                - "8741"
            - name: idle
              long: idle
              positional: false
              help: The started server stops when no peer has been attached for this many seconds
              value_name: SECONDS
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults:
                - "900"
            - name: wait
              long: wait
              positional: false
              help: How long to wait for a server to become ready before reporting what stands
              value_name: SECONDS
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults:
                - "20"
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: false
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: serve-ensure-idle
              title: "Make sure a server serves this checkout, and let it end when idle"
              description: "Starts a server as a process of its own when none answers, waits until it is ready, and prints one line: the standing, the address, the pid. Run again, it finds the server ready and starts nothing. `--idle` is how long the started server outlives its last peer; one second here, so that the example leaves nothing behind."
              argv:
                - serve
                - ensure
                - "--idle"
                - "1"
                - "--wait"
                - "30"
              command: majordomus serve ensure --idle 1 --wait 30
              setup: []
              expectation: exits 0; prints ready
          subcommands: []
        - path:
            - majordomus
            - serve
            - stop
          route: /docs/cli/serve/stop/
          executable: true
          usage: "majordomus serve stop [OPTIONS]"
          about: "Stop this checkout's server — the one its lease names, when it answers for this checkout — and wait for the lease to go"
          args:
            - name: wait
              long: wait
              positional: false
              help: How long to wait for the server to end
              value_name: SECONDS
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults:
                - "10"
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: serve-stop-nothing
              title: "Stop this checkout's server, when there is one"
              description: "Signals the server this checkout's lease names, when it answers for this checkout, and waits for the lease to go. A checkout with no lease has nothing to stop, and says so."
              argv:
                - serve
                - stop
              command: majordomus serve stop
              setup: []
              expectation: exits 0; prints nothing to stop
          subcommands: []
    - path:
        - majordomus
        - capabilities
      route: /docs/cli/capabilities/
      executable: false
      usage: "majordomus capabilities [OPTIONS] <COMMAND>"
      about: "Introspect the capability registry: what exists, where it came from, how it is exposed"
      args:
        - name: repo
          long: repo
          positional: false
          help: "Start the search for the repository root here (default: the current directory)"
          value_name: PATH
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: discovery
          long: discovery
          positional: false
          help: How declarative files are enumerated
          value_name: DISCOVERY
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: vcs
              help: "Tracked files, through the version-control index (the layer's contract)"
            - name: filesystem
              help: A walk of the work tree with the same glob semantics; untracked files included
          defaults:
            - vcs
        - name: strict
          long: strict
          positional: false
          help: Refuse to proceed when any file of the layer carries an error diagnostic
          value_name: STRICT
          takes_value: false
          required: false
          global: true
          possible_values: []
          defaults:
            - "false"
        - name: share
          long: share
          positional: false
          help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
          value_name: DIR
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
      examples: []
      subcommands:
        - path:
            - majordomus
            - capabilities
            - list
          route: /docs/cli/capabilities/list/
          executable: true
          usage: "majordomus capabilities list [OPTIONS]"
          about: "Every capability, one line each, with its projections"
          args:
            - name: kind
              long: kind
              positional: false
              help: "Only this kind: query or resource"
              value_name: KIND
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: exposure
              long: exposure
              positional: false
              help: "Only capabilities exposed through this projection: mcp, http or cli"
              value_name: EXPOSURE
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: false
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: capabilities-list-cli
              title: Which capabilities the command line itself dispatches to
              description: "One line per capability exposed through the `cli` projection, with the projections of each. The registry answers this; no list of capabilities is written in the command line's own declaration."
              argv:
                - capabilities
                - list
                - "--exposure"
                - cli
              command: majordomus capabilities list --exposure cli
              setup: []
              expectation: "exits 0; prints capabilities.list, capabilities.describe"
            - id: capabilities-list-json
              title: Every capability as one JSON document
              description: "The whole registry for a script: each capability with its kind, its provenance and every projection it has."
              argv:
                - capabilities
                - list
                - "--format"
                - json
              command: majordomus capabilities list --format json
              setup: []
              expectation: exits 0; prints one JSON document carrying /capabilities
          subcommands: []
        - path:
            - majordomus
            - capabilities
            - describe
          route: /docs/cli/capabilities/describe/
          executable: true
          usage: "majordomus capabilities describe [OPTIONS] <ID>"
          about: "One capability by canonical id: schemas, provenance, every projection"
          args:
            - name: id
              positional: true
              help: The canonical id
              value_name: ID
              takes_value: true
              required: true
              global: false
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: false
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: capabilities-describe-objects-get
              title: "One capability in full, by its canonical id"
              description: "Its kind, its input and output schemas, where it was composed, and every projection of it: the MCP tool or resource, the HTTP route, the CLI path."
              argv:
                - capabilities
                - describe
                - objects.get
              command: majordomus capabilities describe objects.get
              setup: []
              expectation: "exits 0; prints objects.get, GET /api/v1/object"
          subcommands: []
        - path:
            - majordomus
            - capabilities
            - schema
          route: /docs/cli/capabilities/schema/
          executable: true
          usage: "majordomus capabilities schema [OPTIONS] <ID>"
          about: The canonical input or output JSON Schema of one capability
          args:
            - name: id
              positional: true
              help: The canonical id
              value_name: ID
              takes_value: true
              required: true
              global: false
              possible_values: []
              defaults: []
            - name: side
              long: side
              positional: false
              help: Input or output
              value_name: SIDE
              takes_value: true
              required: false
              global: false
              possible_values:
                - name: input
                  help: The schema of the input
                - name: output
                  help: The schema of the output
              defaults:
                - input
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: capabilities-schema-output
              title: The canonical output schema of a capability
              description: "The JSON Schema the MCP and OpenAPI projections are derived from; `--side input` prints the schema of what the capability accepts."
              argv:
                - capabilities
                - schema
                - objects.get
                - "--side"
                - output
              command: majordomus capabilities schema objects.get --side output
              setup: []
              expectation: exits 0; prints one JSON document carrying /title
          subcommands: []
        - path:
            - majordomus
            - capabilities
            - projections
          route: /docs/cli/capabilities/projections/
          executable: true
          usage: "majordomus capabilities projections [OPTIONS]"
          about: "Where each capability is projected, and every claim its surface does not answer"
          args:
            - name: module
              long: module
              positional: false
              help: Only capabilities composed in this module
              value_name: MODULE
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: unmet
              long: unmet
              positional: false
              help: Only the capabilities whose declared exposures are not all answered
              value_name: UNMET
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: false
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: capabilities-projections-unmet
              title: Every exposure a capability claims that its surface does not answer
              description: "`rows: 0` is the closure `project.interfaces-are-projections` asks for: every declared command line, route and tool is answered by the surface that carries it. The commands no capability claims are reported beside it, as the measure of how much of the command line is still hand-written."
              argv:
                - capabilities
                - projections
                - "--unmet"
              command: majordomus capabilities projections --unmet
              setup: []
              expectation: exits 0
            - id: capabilities-projections-module
              title: "Where one module's capabilities appear"
              description: "A row per capability with the command line, HTTP route and MCP tool it reaches, so a capability that exists but is reachable from nowhere is visible as one."
              argv:
                - capabilities
                - projections
                - "--module"
                - worktree
              command: majordomus capabilities projections --module worktree
              setup: []
              expectation: "exits 0; prints worktree.topology, majordomus worktree topology"
          subcommands: []
        - path:
            - majordomus
            - capabilities
            - validate
          route: /docs/cli/capabilities/validate/
          executable: true
          usage: "majordomus capabilities validate [OPTIONS]"
          about: Build the registry and every projection; exit 10 with every violation named
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: capabilities-validate
              title: Prove the registry and every projection of it
              description: "Builds the registry, the MCP and HTTP surfaces, the OpenAPI document, the command line's documentation and the benchmark coverage, and names every failure. Exit 10 when anything is unmet."
              argv:
                - capabilities
                - validate
              command: majordomus capabilities validate
              setup: []
              expectation: "exits 0; prints validate: 0 failure(s), OK   cli"
          subcommands: []
    - path:
        - majordomus
        - generate
      route: /docs/cli/generate/
      executable: true
      usage: "majordomus generate [OPTIONS] [TARGET]"
      about: "Write the committed projections of the registry (docs/generated), or check that they are current"
      args:
        - name: repo
          long: repo
          positional: false
          help: "Start the search for the repository root here (default: the current directory)"
          value_name: PATH
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: discovery
          long: discovery
          positional: false
          help: How declarative files are enumerated
          value_name: DISCOVERY
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: vcs
              help: "Tracked files, through the version-control index (the layer's contract)"
            - name: filesystem
              help: A walk of the work tree with the same glob semantics; untracked files included
          defaults:
            - vcs
        - name: strict
          long: strict
          positional: false
          help: Refuse to proceed when any file of the layer carries an error diagnostic
          value_name: STRICT
          takes_value: false
          required: false
          global: true
          possible_values: []
          defaults:
            - "false"
        - name: share
          long: share
          positional: false
          help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
          value_name: DIR
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: target
          positional: true
          help: What to generate
          value_name: TARGET
          takes_value: true
          required: false
          global: false
          possible_values:
            - name: all
              help: Every target
            - name: openapi
              help: "`docs/generated/openapi.{json,yaml}`"
            - name: docs
              help: "`docs/generated/capabilities.md`, `docs/generated/modules/<id>.md` and `docs/generated/cli.{md,json,yaml}`"
            - name: benchmarks
              help: "`docs/generated/benchmarks.{md,json,yaml}`: every benchmark target and the coverage"
            - name: registry
              help: "`docs/generated/registry.{json,yaml}`: the builtin registry as data"
            - name: allow
              help: "The shell tool's allow-lists under share/allow, derived from the schemas"
            - name: providers
              help: "The provider bootstraps the policy declares (AGENTS.md, CLAUDE.md, ...)"
            - name: site
              help: "site/data/registry/registry.json, the registry dataset the site renders"
            - name: manifest
              help: "docs/generated/artifacts.{json,yaml,md}: the index of every generated artifact"
            - name: distribution
              help: "The installer, the installation guide, the release build matrix and the public release metadata, from share/distribution.yaml and .ai/repo/releases/"
            - name: web
              help: "`docs/generated/web.json`: the resolved web topology the site's route reference renders"
            - name: changelog
              help: "`docs/generated/changelog.{json,yaml,md}`: the changelog composed from the layer's release records, its decisions and the repository's commits"
            - name: deployment
              help: "deploy/Dockerfile, .dockerignore and fly.toml, from the deployment objects"
            - name: graph
              help: "docs/generated/graph.json and its schema: the composed graph as data"
            - name: design
              help: "The design system's projections, from share/design/tokens.yaml: the stylesheets both Tailwind builds import, the tokens and the declaration compiled into the crate, every copy of the brand, site/data/registry/design.json and docs/generated/design.*"
          defaults:
            - all
        - name: check
          long: check
          positional: false
          help: Compare with what is on disk and exit 10 when stale; write nothing
          value_name: CHECK
          takes_value: false
          required: false
          global: false
          possible_values: []
          defaults:
            - "false"
        - name: out
          long: out
          positional: false
          help: Write under this directory instead of the repository root (docs/generated is appended)
          value_name: DIR
          takes_value: true
          required: false
          global: false
          possible_values: []
          defaults: []
      examples:
        - id: generate-all
          title: Write every committed projection
          description: "The OpenAPI document, the capability reference, the command-line reference and its JSON, the registry manifest, the benchmark matrix, the shell tool's allow-lists, the provider bootstraps and the site's registry dataset — all from the one registry and the one clap declaration."
          argv:
            - generate
          command: majordomus generate
          setup: []
          expectation: exits 0
        - id: generate-check
          title: Refuse a tree whose projections are stale
          description: "Writes nothing and compares instead: exit 0 when every committed projection is what the sources produce, exit 10 with each stale file named. This is the form CI runs."
          argv:
            - generate
            - "--check"
          command: majordomus generate --check
          setup:
            - argv:
                - generate
              command: majordomus generate
          expectation: exits 0
        - id: generate-one-target
          title: One target only
          description: "Each target can be written on its own while a change is iterated on; `majordomus generate` with no target writes all of them."
          argv:
            - generate
            - openapi
          command: majordomus generate openapi
          setup: []
          expectation: exits 0
      subcommands: []
    - path:
        - majordomus
        - bench
      route: /docs/cli/bench/
      executable: true
      usage: "majordomus bench [OPTIONS] [COMMAND] [ID]"
      about: "Time every externally callable operation (each capability directly, over MCP and over HTTP, and the transports' own operations), report coverage, compare with the accepted baseline"
      args:
        - name: repo
          long: repo
          positional: false
          help: "Start the search for the repository root here (default: the current directory)"
          value_name: PATH
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: discovery
          long: discovery
          positional: false
          help: How declarative files are enumerated
          value_name: DISCOVERY
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: vcs
              help: "Tracked files, through the version-control index (the layer's contract)"
            - name: filesystem
              help: A walk of the work tree with the same glob semantics; untracked files included
          defaults:
            - vcs
        - name: strict
          long: strict
          positional: false
          help: Refuse to proceed when any file of the layer carries an error diagnostic
          value_name: STRICT
          takes_value: false
          required: false
          global: true
          possible_values: []
          defaults:
            - "false"
        - name: share
          long: share
          positional: false
          help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
          value_name: DIR
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: id
          positional: true
          help: "Only targets of this capability id, or whose key starts with this text"
          value_name: ID
          takes_value: true
          required: false
          global: false
          possible_values: []
          defaults: []
        - name: transport
          long: transport
          positional: false
          help: Only this transport
          value_name: TRANSPORT
          takes_value: true
          required: false
          global: false
          possible_values:
            - name: all
              help: Every target
            - name: direct
              help: "Capabilities through the executor, in process"
            - name: mcp
              help: "Capabilities through a real `majordomus mcp` child"
            - name: http
              help: Capabilities over a real loopback socket
            - name: system
              help: "The transports' own operations only"
          defaults:
            - all
        - name: profile
          long: profile
          positional: false
          help: How much to measure
          value_name: PROFILE
          takes_value: true
          required: false
          global: false
          possible_values:
            - name: quick
              help: "Fast developer feedback: few samples"
            - name: full
              help: "Stable evidence: many samples, many cold spawns"
            - name: ci
              help: "Conservative: structural gates plus a modest measurement"
          defaults:
            - quick
        - name: format
          long: format
          positional: false
          help: Output shape
          value_name: FORMAT
          takes_value: true
          required: false
          global: false
          possible_values:
            - name: text
              help: Lines for a person
            - name: json
              help: "One JSON document, deterministic"
          defaults:
            - text
        - name: check
          long: check
          positional: false
          help: Compare with the accepted baseline of this platform under .ai/repo/benchmarks/rust/policy.yaml; exit 10 on a regression
          value_name: CHECK
          takes_value: false
          required: false
          global: false
          possible_values: []
          defaults:
            - "false"
        - name: no_write
          long: no-write
          positional: false
          help: Do not write the result under .ai/local/benchmarks/
          value_name: NO_WRITE
          takes_value: false
          required: false
          global: false
          possible_values: []
          defaults:
            - "false"
      examples:
        - id: bench-direct-quick
          title: Time the capabilities in process
          description: "The quick profile takes few samples, and `--transport direct` measures the executor without spawning a server. Nothing is written under .ai/local/ with `--no-write`."
          argv:
            - bench
            - "--transport"
            - direct
            - "--profile"
            - quick
            - "--no-write"
            - "--format"
            - json
          command: majordomus bench --transport direct --profile quick --no-write --format json
          setup: []
          expectation: "exits 0; prints one JSON document carrying /results, /profile"
      subcommands:
        - path:
            - majordomus
            - bench
            - coverage
          route: /docs/cli/bench/coverage/
          executable: true
          usage: "majordomus bench coverage [OPTIONS]"
          about: Every required target and whether it is covered; the denominator is generated from the registry
          args:
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: false
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
            - name: check
              long: check
              positional: false
              help: Exit 10 when any required target is missing or waived
              value_name: CHECK
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: bench-coverage-json
              title: Every required benchmark target and whether it is covered
              description: "The denominator is generated from the registry: every executable capability, on every transport it is exposed on, plus the transports' own operations."
              argv:
                - bench
                - coverage
                - "--format"
                - json
              command: majordomus bench coverage --format json
              setup: []
              expectation: "exits 0; prints one JSON document carrying /lines, /tallies"
            - id: bench-coverage-check
              title: Fail when a target is missing
              description: "Exit 10 when any required target is uncovered or waived, so a capability that nothing times cannot be merged."
              argv:
                - bench
                - coverage
                - "--check"
              command: majordomus bench coverage --check
              setup: []
              expectation: exits 0
          subcommands: []
        - path:
            - majordomus
            - bench
            - baseline
          route: /docs/cli/bench/baseline/
          executable: false
          usage: "majordomus bench baseline [OPTIONS] <COMMAND>"
          about: The accepted baseline of this platform under .ai/repo/benchmarks/rust/
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples: []
          subcommands:
            - path:
                - majordomus
                - bench
                - baseline
                - update
              route: /docs/cli/bench/baseline/update/
              executable: true
              usage: "majordomus bench baseline update [OPTIONS]"
              about: "Run the benchmarks and record them as this platform's baseline (a reviewable, tracked file)"
              args:
                - name: profile
                  long: profile
                  positional: false
                  help: How much to measure
                  value_name: PROFILE
                  takes_value: true
                  required: false
                  global: false
                  possible_values:
                    - name: quick
                      help: "Fast developer feedback: few samples"
                    - name: full
                      help: "Stable evidence: many samples, many cold spawns"
                    - name: ci
                      help: "Conservative: structural gates plus a modest measurement"
                  defaults:
                    - full
                - name: allow_dirty
                  long: allow-dirty
                  positional: false
                  help: Record even from a dirty work tree
                  value_name: ALLOW_DIRTY
                  takes_value: false
                  required: false
                  global: false
                  possible_values: []
                  defaults:
                    - "false"
                - name: repo
                  long: repo
                  positional: false
                  help: "Start the search for the repository root here (default: the current directory)"
                  value_name: PATH
                  takes_value: true
                  required: false
                  global: true
                  possible_values: []
                  defaults: []
                - name: discovery
                  long: discovery
                  positional: false
                  help: How declarative files are enumerated
                  value_name: DISCOVERY
                  takes_value: true
                  required: false
                  global: true
                  possible_values:
                    - name: vcs
                      help: "Tracked files, through the version-control index (the layer's contract)"
                    - name: filesystem
                      help: A walk of the work tree with the same glob semantics; untracked files included
                  defaults:
                    - vcs
                - name: strict
                  long: strict
                  positional: false
                  help: Refuse to proceed when any file of the layer carries an error diagnostic
                  value_name: STRICT
                  takes_value: false
                  required: false
                  global: true
                  possible_values: []
                  defaults:
                    - "false"
                - name: share
                  long: share
                  positional: false
                  help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
                  value_name: DIR
                  takes_value: true
                  required: false
                  global: true
                  possible_values: []
                  defaults: []
              examples:
                - id: bench-baseline-update-quick
                  title: "Record this platform's accepted baseline"
                  description: "Runs the benchmarks and writes the result under .ai/repo/benchmarks/rust/ as a tracked, reviewable file. The full profile is the default; the quick profile is for trying the path out. A dirty work tree is refused unless --allow-dirty says otherwise."
                  argv:
                    - bench
                    - baseline
                    - update
                    - "--profile"
                    - quick
                    - "--allow-dirty"
                  command: majordomus bench baseline update --profile quick --allow-dirty
                  setup: []
                  expectation: exits 0
              subcommands: []
    - path:
        - majordomus
        - scope
      route: /docs/cli/scope/
      executable: true
      usage: "majordomus scope [OPTIONS] [PATHS]"
      about: "The repository scope: what a worker reads and what it never reads; with paths, whether each is in or out and why"
      args:
        - name: repo
          long: repo
          positional: false
          help: "Start the search for the repository root here (default: the current directory)"
          value_name: PATH
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: discovery
          long: discovery
          positional: false
          help: How declarative files are enumerated
          value_name: DISCOVERY
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: vcs
              help: "Tracked files, through the version-control index (the layer's contract)"
            - name: filesystem
              help: A walk of the work tree with the same glob semantics; untracked files included
          defaults:
            - vcs
        - name: strict
          long: strict
          positional: false
          help: Refuse to proceed when any file of the layer carries an error diagnostic
          value_name: STRICT
          takes_value: false
          required: false
          global: true
          possible_values: []
          defaults:
            - "false"
        - name: share
          long: share
          positional: false
          help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
          value_name: DIR
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: paths
          positional: true
          help: Repository-relative paths to judge; none prints the declaration and the tally
          value_name: PATHS
          takes_value: true
          required: false
          global: false
          possible_values: []
          defaults: []
        - name: format
          long: format
          positional: false
          help: Output shape
          value_name: FORMAT
          takes_value: true
          required: false
          global: false
          possible_values:
            - name: text
              help: Lines for a person
            - name: json
              help: "One JSON document, deterministic"
          defaults:
            - text
        - name: check
          long: check
          positional: false
          help: Exit 10 when any path given is out of the scope
          value_name: CHECK
          takes_value: false
          required: false
          global: false
          possible_values: []
          defaults:
            - "false"
      examples:
        - id: scope-declaration
          title: What a worker reads of this repository
          description: "With no path, the declaration itself and the tally: how many tracked files are in the scope and how many are out."
          argv:
            - scope
          command: majordomus scope
          setup: []
          expectation: exits 0
        - id: scope-paths-json
          title: "Judge paths, and say which rule decided"
          description: "For each path: in or out, and the rule that decided it. `--check` exits 10 when any path given is out, which is how a hook refuses to read one."
          argv:
            - scope
            - docs/CLI.md
            - "--format"
            - json
          command: majordomus scope docs/CLI.md --format json
          setup: []
          expectation: "exits 0; prints one JSON document carrying /0/verdict, /0/rule"
      subcommands: []
    - path:
        - majordomus
        - web
      route: /docs/cli/web/
      executable: true
      usage: "majordomus web [OPTIONS] [COMMAND]"
      about: "The repository's web surfaces: what is exposed, where it is mounted, what produced it, and whether the topology is valid"
      args:
        - name: repo
          long: repo
          positional: false
          help: "Start the search for the repository root here (default: the current directory)"
          value_name: PATH
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: discovery
          long: discovery
          positional: false
          help: How declarative files are enumerated
          value_name: DISCOVERY
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: vcs
              help: "Tracked files, through the version-control index (the layer's contract)"
            - name: filesystem
              help: A walk of the work tree with the same glob semantics; untracked files included
          defaults:
            - vcs
        - name: strict
          long: strict
          positional: false
          help: Refuse to proceed when any file of the layer carries an error diagnostic
          value_name: STRICT
          takes_value: false
          required: false
          global: true
          possible_values: []
          defaults:
            - "false"
        - name: share
          long: share
          positional: false
          help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
          value_name: DIR
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: format
          long: format
          positional: false
          help: Output shape
          value_name: FORMAT
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: text
              help: Lines for a person
            - name: json
              help: "One JSON document, deterministic"
          defaults:
            - text
        - name: only
          long: only
          positional: false
          help: "Only these surfaces, by discovered id (repeat or separate with commas)"
          value_name: ONLY
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: exclude
          long: exclude
          positional: false
          help: "Every surface except these, by discovered id"
          value_name: EXCLUDE
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
      examples:
        - id: web-topology
          title: "The topology, from the command with no subcommand"
          description: "`web` with nothing after it lists, because listing is what a person wants when they ask what this repository exposes."
          argv:
            - web
          command: majordomus web
          setup: []
          expectation: "exits 0; prints ID, MOUNT"
      subcommands:
        - path:
            - majordomus
            - web
            - list
          route: /docs/cli/web/list/
          executable: true
          usage: "majordomus web list [OPTIONS]"
          about: "Every discovered surface: id, kind, mount, producer"
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
            - name: only
              long: only
              positional: false
              help: "Only these surfaces, by discovered id (repeat or separate with commas)"
              value_name: ONLY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: exclude
              long: exclude
              positional: false
              help: "Every surface except these, by discovered id"
              value_name: EXCLUDE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: web-list
              title: Every web surface this repository exposes
              description: "The resolved topology, in route-precedence order: the routes the executable answers itself, the application's site, and every generated report that declared itself under the generated web root. Nothing is registered anywhere; each line was discovered."
              argv:
                - web
                - list
              command: majordomus web list
              setup: []
              expectation: "exits 0; prints MOUNT, /api/v1, /swagger"
          subcommands: []
        - path:
            - majordomus
            - web
            - explain
          route: /docs/cli/web/explain/
          executable: true
          usage: "majordomus web explain [OPTIONS] [ID]"
          about: Why each surface exists and where each of its values came from
          args:
            - name: id
              positional: true
              help: Only this surface; none explains every one
              value_name: ID
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
            - name: only
              long: only
              positional: false
              help: "Only these surfaces, by discovered id (repeat or separate with commas)"
              value_name: ONLY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: exclude
              long: exclude
              positional: false
              help: "Every surface except these, by discovered id"
              value_name: EXCLUDE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: web-explain
              title: Why a surface exists and where each of its values came from
              description: "For each field a reader could be surprised by — the mount, the kind, the directory — the source that decided it: a producer's own declaration, the site configuration, the capability registry, or the model's documented default."
              argv:
                - web
                - explain
                - swagger
              command: majordomus web explain swagger
              setup: []
              expectation: "exits 0; prints swagger, came from"
          subcommands: []
        - path:
            - majordomus
            - web
            - validate
          route: /docs/cli/web/validate/
          executable: true
          usage: "majordomus web validate [OPTIONS]"
          about: "Check the topology's invariants; exit 10 on any error finding"
          args:
            - name: artifacts
              long: artifacts
              positional: false
              help: "Also require every static surface's directory and index to exist"
              value_name: ARTIFACTS
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
            - name: only
              long: only
              positional: false
              help: "Only these surfaces, by discovered id (repeat or separate with commas)"
              value_name: ONLY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: exclude
              long: exclude
              positional: false
              help: "Every surface except these, by discovered id"
              value_name: EXCLUDE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: web-validate
              title: Check the topology before anything serves or publishes it
              description: "Two surfaces claiming one path, a surface nested inside another's subtree, a directory outside the generated root or one that walks out of the repository: each is a named finding with the surface, the value, its source and the fix. Exit 10 on any error finding. `--artifacts` also requires every static surface's directory and index to exist, which is what serving and publishing need."
              argv:
                - web
                - validate
              command: majordomus web validate
              setup: []
              expectation: exits 0; prints no conflict
          subcommands: []
        - path:
            - majordomus
            - web
            - manifest
          route: /docs/cli/web/manifest/
          executable: true
          usage: "majordomus web manifest [OPTIONS]"
          about: Write the resolved topology to the generated manifest
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
            - name: only
              long: only
              positional: false
              help: "Only these surfaces, by discovered id (repeat or separate with commas)"
              value_name: ONLY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: exclude
              long: exclude
              positional: false
              help: "Every surface except these, by discovered id"
              value_name: EXCLUDE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: web-manifest
              title: Write the resolved topology down for another tool to read
              description: "The manifest under the generated web root is derived state: a publisher or a CI job may read it instead of resolving the topology again, and nothing may edit it, because the next run overwrites it from the same discovery."
              argv:
                - web
                - manifest
              command: majordomus web manifest
              setup: []
              expectation: "exits 0; prints web manifest, surface"
          subcommands: []
        - path:
            - majordomus
            - web
            - report
          route: /docs/cli/web/report/
          executable: false
          usage: "majordomus web report [OPTIONS] <COMMAND>"
          about: Render a generated report into its own surface under the generated web root
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
            - name: only
              long: only
              positional: false
              help: "Only these surfaces, by discovered id (repeat or separate with commas)"
              value_name: ONLY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: exclude
              long: exclude
              positional: false
              help: "Every surface except these, by discovered id"
              value_name: EXCLUDE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples: []
          subcommands:
            - path:
                - majordomus
                - web
                - report
                - tests
              route: /docs/cli/web/report/tests/
              executable: true
              usage: "majordomus web report tests [OPTIONS]"
              about: "The test run: the behavioural cases' report, and the crate's own totals"
              args:
                - name: suite
                  long: suite
                  positional: false
                  help: "The runner's TSV report (`MJ_TEST_REPORT=<file> bash test/run.sh`)"
                  value_name: SUITE
                  takes_value: true
                  required: true
                  global: false
                  possible_values: []
                  defaults: []
                - name: crate_output
                  long: crate-output
                  positional: false
                  help: "The output of `cargo test`, for its totals"
                  value_name: CRATE_OUTPUT
                  takes_value: true
                  required: false
                  global: false
                  possible_values: []
                  defaults: []
                - name: repo
                  long: repo
                  positional: false
                  help: "Start the search for the repository root here (default: the current directory)"
                  value_name: PATH
                  takes_value: true
                  required: false
                  global: true
                  possible_values: []
                  defaults: []
                - name: discovery
                  long: discovery
                  positional: false
                  help: How declarative files are enumerated
                  value_name: DISCOVERY
                  takes_value: true
                  required: false
                  global: true
                  possible_values:
                    - name: vcs
                      help: "Tracked files, through the version-control index (the layer's contract)"
                    - name: filesystem
                      help: A walk of the work tree with the same glob semantics; untracked files included
                  defaults:
                    - vcs
                - name: strict
                  long: strict
                  positional: false
                  help: Refuse to proceed when any file of the layer carries an error diagnostic
                  value_name: STRICT
                  takes_value: false
                  required: false
                  global: true
                  possible_values: []
                  defaults:
                    - "false"
                - name: share
                  long: share
                  positional: false
                  help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
                  value_name: DIR
                  takes_value: true
                  required: false
                  global: true
                  possible_values: []
                  defaults: []
                - name: format
                  long: format
                  positional: false
                  help: Output shape
                  value_name: FORMAT
                  takes_value: true
                  required: false
                  global: true
                  possible_values:
                    - name: text
                      help: Lines for a person
                    - name: json
                      help: "One JSON document, deterministic"
                  defaults:
                    - text
                - name: only
                  long: only
                  positional: false
                  help: "Only these surfaces, by discovered id (repeat or separate with commas)"
                  value_name: ONLY
                  takes_value: true
                  required: false
                  global: true
                  possible_values: []
                  defaults: []
                - name: exclude
                  long: exclude
                  positional: false
                  help: "Every surface except these, by discovered id"
                  value_name: EXCLUDE
                  takes_value: true
                  required: false
                  global: true
                  possible_values: []
                  defaults: []
              examples:
                - id: web-report-tests
                  title: "Render the suite's own results into the /tests surface"
                  description: "The runner writes its report with `MJ_TEST_REPORT=<file> bash test/run.sh`; this renders it, keeps the machine-readable results beside the page, and declares the directory so discovery finds it. Without that file there is nothing to render and the command says so rather than publishing an empty page."
                  argv:
                    - web
                    - report
                    - tests
                    - "--suite"
                    - target/web/run.tsv
                  command: majordomus web report tests --suite target/web/run.tsv
                  setup: []
                  expectation: exits 13
              subcommands: []
            - path:
                - majordomus
                - web
                - report
                - benchmarks
              route: /docs/cli/web/report/benchmarks/
              executable: true
              usage: "majordomus web report benchmarks [OPTIONS]"
              about: "The benchmark run: a results document, or the accepted baseline"
              args:
                - name: from
                  long: from
                  positional: false
                  help: "A results document from `majordomus bench`, or a baseline under the layer"
                  value_name: FROM
                  takes_value: true
                  required: true
                  global: false
                  possible_values: []
                  defaults: []
                - name: repo
                  long: repo
                  positional: false
                  help: "Start the search for the repository root here (default: the current directory)"
                  value_name: PATH
                  takes_value: true
                  required: false
                  global: true
                  possible_values: []
                  defaults: []
                - name: discovery
                  long: discovery
                  positional: false
                  help: How declarative files are enumerated
                  value_name: DISCOVERY
                  takes_value: true
                  required: false
                  global: true
                  possible_values:
                    - name: vcs
                      help: "Tracked files, through the version-control index (the layer's contract)"
                    - name: filesystem
                      help: A walk of the work tree with the same glob semantics; untracked files included
                  defaults:
                    - vcs
                - name: strict
                  long: strict
                  positional: false
                  help: Refuse to proceed when any file of the layer carries an error diagnostic
                  value_name: STRICT
                  takes_value: false
                  required: false
                  global: true
                  possible_values: []
                  defaults:
                    - "false"
                - name: share
                  long: share
                  positional: false
                  help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
                  value_name: DIR
                  takes_value: true
                  required: false
                  global: true
                  possible_values: []
                  defaults: []
                - name: format
                  long: format
                  positional: false
                  help: Output shape
                  value_name: FORMAT
                  takes_value: true
                  required: false
                  global: true
                  possible_values:
                    - name: text
                      help: Lines for a person
                    - name: json
                      help: "One JSON document, deterministic"
                  defaults:
                    - text
                - name: only
                  long: only
                  positional: false
                  help: "Only these surfaces, by discovered id (repeat or separate with commas)"
                  value_name: ONLY
                  takes_value: true
                  required: false
                  global: true
                  possible_values: []
                  defaults: []
                - name: exclude
                  long: exclude
                  positional: false
                  help: "Every surface except these, by discovered id"
                  value_name: EXCLUDE
                  takes_value: true
                  required: false
                  global: true
                  possible_values: []
                  defaults: []
              examples:
                - id: web-report-benchmarks
                  title: Render a benchmark run into the /benchmarks surface
                  description: "Reads a results document — a run's own output, or an accepted baseline under the layer's benchmarks section, which have the same shape — and renders every measured target ordered by median. It measures nothing itself: a figure on the page is a figure a run produced."
                  argv:
                    - web
                    - report
                    - benchmarks
                    - "--from"
                    - .ai/repo/benchmarks/rust/baseline.macos-aarch64-debug.json
                  command: majordomus web report benchmarks --from .ai/repo/benchmarks/rust/baseline.macos-aarch64-debug.json
                  setup: []
                  expectation: exits 13
              subcommands: []
            - path:
                - majordomus
                - web
                - report
                - ui
              route: /docs/cli/web/report/ui/
              executable: true
              usage: "majordomus web report ui [OPTIONS]"
              about: "The UI conformance audit, rendered as a section of the test surface (/tests/ui)"
              args:
                - name: from
                  long: from
                  positional: false
                  help: "A results document from `scripts/ui audit`"
                  value_name: FROM
                  takes_value: true
                  required: true
                  global: false
                  possible_values: []
                  defaults: []
                - name: repo
                  long: repo
                  positional: false
                  help: "Start the search for the repository root here (default: the current directory)"
                  value_name: PATH
                  takes_value: true
                  required: false
                  global: true
                  possible_values: []
                  defaults: []
                - name: discovery
                  long: discovery
                  positional: false
                  help: How declarative files are enumerated
                  value_name: DISCOVERY
                  takes_value: true
                  required: false
                  global: true
                  possible_values:
                    - name: vcs
                      help: "Tracked files, through the version-control index (the layer's contract)"
                    - name: filesystem
                      help: A walk of the work tree with the same glob semantics; untracked files included
                  defaults:
                    - vcs
                - name: strict
                  long: strict
                  positional: false
                  help: Refuse to proceed when any file of the layer carries an error diagnostic
                  value_name: STRICT
                  takes_value: false
                  required: false
                  global: true
                  possible_values: []
                  defaults:
                    - "false"
                - name: share
                  long: share
                  positional: false
                  help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
                  value_name: DIR
                  takes_value: true
                  required: false
                  global: true
                  possible_values: []
                  defaults: []
                - name: format
                  long: format
                  positional: false
                  help: Output shape
                  value_name: FORMAT
                  takes_value: true
                  required: false
                  global: true
                  possible_values:
                    - name: text
                      help: Lines for a person
                    - name: json
                      help: "One JSON document, deterministic"
                  defaults:
                    - text
                - name: only
                  long: only
                  positional: false
                  help: "Only these surfaces, by discovered id (repeat or separate with commas)"
                  value_name: ONLY
                  takes_value: true
                  required: false
                  global: true
                  possible_values: []
                  defaults: []
                - name: exclude
                  long: exclude
                  positional: false
                  help: "Every surface except these, by discovered id"
                  value_name: EXCLUDE
                  takes_value: true
                  required: false
                  global: true
                  possible_values: []
                  defaults: []
              examples:
                - id: web-report-ui
                  title: Render the UI conformance audit into /tests/ui
                  description: "`scripts/ui audit` drives a browser over every page of the built site at every width the compiled stylesheet's breakpoints imply, and writes one results document; this renders it. The rendering is a section of the test surface rather than a surface of its own, because a conformance run is a test run and the topology refuses a surface mounted inside another's subtree. Without that document there is nothing to render and the command says so."
                  argv:
                    - web
                    - report
                    - ui
                    - "--from"
                    - target/web/run-ui.json
                  command: majordomus web report ui --from target/web/run-ui.json
                  setup: []
                  expectation: exits 13
              subcommands: []
        - path:
            - majordomus
            - web
            - compose
          route: /docs/cli/web/compose/
          executable: true
          usage: "majordomus web compose [OPTIONS]"
          about: Compose every published surface into one publishable tree
          args:
            - name: destination
              long: destination
              positional: false
              help: Where to write it; the default is target/site
              value_name: DESTINATION
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
            - name: only
              long: only
              positional: false
              help: "Only these surfaces, by discovered id (repeat or separate with commas)"
              value_name: ONLY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: exclude
              long: exclude
              positional: false
              help: "Every surface except these, by discovered id"
              value_name: EXCLUDE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: web-compose
              title: Compose every published surface into one publishable tree
              description: "Each producer owns its own output directory; publication needs one tree, and the mapping is the resolved mount and nothing else. A surface that is discovered is published without a copy step being written anywhere, and a repository with nothing generated yet composes an empty tree rather than an error. Where a surface exists and its directory does not, composition refuses and names the producer to run."
              argv:
                - web
                - compose
                - "--destination"
                - target/site
              command: majordomus web compose --destination target/site
              setup: []
              expectation: exits 0
          subcommands: []
    - path:
        - majordomus
        - why
      route: /docs/cli/why/
      executable: true
      usage: "majordomus why [OPTIONS] [COMMAND]"
      about: "The operational moments this tool answers: the catalogue, one moment, the audiences and areas, a diagnosis of your own week, and the catalogue's own validation"
      args:
        - name: repo
          long: repo
          positional: false
          help: "Start the search for the repository root here (default: the current directory)"
          value_name: PATH
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: discovery
          long: discovery
          positional: false
          help: How declarative files are enumerated
          value_name: DISCOVERY
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: vcs
              help: "Tracked files, through the version-control index (the layer's contract)"
            - name: filesystem
              help: A walk of the work tree with the same glob semantics; untracked files included
          defaults:
            - vcs
        - name: strict
          long: strict
          positional: false
          help: Refuse to proceed when any file of the layer carries an error diagnostic
          value_name: STRICT
          takes_value: false
          required: false
          global: true
          possible_values: []
          defaults:
            - "false"
        - name: share
          long: share
          positional: false
          help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
          value_name: DIR
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: format
          long: format
          positional: false
          help: Output shape
          value_name: FORMAT
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: text
              help: Lines for a person
            - name: json
              help: "One JSON document, deterministic"
          defaults:
            - text
        - name: audience
          long: audience
          positional: false
          help: Only moments this audience recognises
          value_name: AUDIENCE
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: area
          long: area
          positional: false
          help: Only moments in this operational area
          value_name: AREA
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: tag
          long: tag
          positional: false
          help: Only moments carrying this tag
          value_name: TAG
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: severity
          long: severity
          positional: false
          help: Only moments of this severity
          value_name: SEVERITY
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: frequency
          long: frequency
          positional: false
          help: Only moments of this frequency
          value_name: FREQUENCY
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: lifecycle
          long: lifecycle
          positional: false
          help: Only moments at this stage of work
          value_name: LIFECYCLE
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: capability
          long: capability
          positional: false
          help: Only moments naming this capability of the executable
          value_name: CAPABILITY
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: names_command
          long: names-command
          positional: false
          help: Only moments naming this command
          value_name: NAMES_COMMAND
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: featured
          long: featured
          positional: false
          help: Only the moments the homepage features
          value_name: FEATURED
          takes_value: false
          required: false
          global: true
          possible_values: []
          defaults:
            - "false"
        - name: all
          long: all
          positional: false
          help: "Include drafts and deprecated moments, not only the public ones"
          value_name: ALL
          takes_value: false
          required: false
          global: true
          possible_values: []
          defaults:
            - "false"
        - name: query
          long: query
          short: q
          positional: false
          help: "Case-insensitive text over identities, titles, hooks, summaries, tags, aliases, signals, examples and bodies"
          value_name: QUERY
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
      examples:
        - id: why-catalogue
          title: The operational moments this repository holds
          description: "`why` with nothing after it lists, because listing is what a person wants when they ask what this section is. The count on the last line is computed from the catalogue; no number anywhere is written down."
          argv:
            - why
          command: majordomus why
          setup: []
          expectation: "exits 0; prints SLUG, moment(s)"
      subcommands:
        - path:
            - majordomus
            - why
            - list
          route: /docs/cli/why/list/
          executable: true
          usage: "majordomus why list [OPTIONS]"
          about: "Every operational moment, narrowed by any facet the catalogue reports"
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
            - name: audience
              long: audience
              positional: false
              help: Only moments this audience recognises
              value_name: AUDIENCE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: area
              long: area
              positional: false
              help: Only moments in this operational area
              value_name: AREA
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: tag
              long: tag
              positional: false
              help: Only moments carrying this tag
              value_name: TAG
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: severity
              long: severity
              positional: false
              help: Only moments of this severity
              value_name: SEVERITY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: frequency
              long: frequency
              positional: false
              help: Only moments of this frequency
              value_name: FREQUENCY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: lifecycle
              long: lifecycle
              positional: false
              help: Only moments at this stage of work
              value_name: LIFECYCLE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: capability
              long: capability
              positional: false
              help: Only moments naming this capability of the executable
              value_name: CAPABILITY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: names_command
              long: names-command
              positional: false
              help: Only moments naming this command
              value_name: NAMES_COMMAND
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: featured
              long: featured
              positional: false
              help: Only the moments the homepage features
              value_name: FEATURED
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: all
              long: all
              positional: false
              help: "Include drafts and deprecated moments, not only the public ones"
              value_name: ALL
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: query
              long: query
              short: q
              positional: false
              help: "Case-insensitive text over identities, titles, hooks, summaries, tags, aliases, signals, examples and bodies"
              value_name: QUERY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: why-list
              title: "Every public moment, in presentation order"
              description: "Drafts are excluded unless `--all` is given. The facets a listing may be narrowed by are the ones the catalogue itself reports, so an audience or an area added as a file is a filter without anything being registered."
              argv:
                - why
                - list
              command: majordomus why list
              setup: []
              expectation: exits 0; prints SLUG
            - id: why-list-audience
              title: Only what one audience recognises
              description: "Membership is declared by each moment and never listed in the audience's own file, so this answer is derived. An audience the catalogue does not have is an invalid input naming the ones it does, not an empty answer."
              argv:
                - why
                - list
                - "--audience"
                - fixture-team
              command: majordomus why list --audience fixture-team
              setup: []
              expectation: exits 0; prints SLUG
            - id: why-list-json
              title: "The same, as the shape the API and MCP answer with"
              description: "One domain model behind every projection: this document is what `GET /api/v1/why` returns and what the `majordomus_why` tool answers, including the derived facets and the catalogue's fingerprint."
              argv:
                - why
                - list
                - "--format"
                - json
              command: majordomus why list --format json
              setup: []
              expectation: "exits 0; prints one JSON document carrying /counts/moments, /facets/audiences, /fingerprint"
          subcommands: []
        - path:
            - majordomus
            - why
            - show
          route: /docs/cli/why/show/
          executable: true
          usage: "majordomus why show [OPTIONS] <ID>"
          about: "One moment in full, with every relation derived from its metadata"
          args:
            - name: id
              positional: true
              help: "The moment's id, which is also its slug and its route"
              value_name: ID
              takes_value: true
              required: true
              global: false
              possible_values: []
              defaults: []
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
            - name: audience
              long: audience
              positional: false
              help: Only moments this audience recognises
              value_name: AUDIENCE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: area
              long: area
              positional: false
              help: Only moments in this operational area
              value_name: AREA
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: tag
              long: tag
              positional: false
              help: Only moments carrying this tag
              value_name: TAG
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: severity
              long: severity
              positional: false
              help: Only moments of this severity
              value_name: SEVERITY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: frequency
              long: frequency
              positional: false
              help: Only moments of this frequency
              value_name: FREQUENCY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: lifecycle
              long: lifecycle
              positional: false
              help: Only moments at this stage of work
              value_name: LIFECYCLE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: capability
              long: capability
              positional: false
              help: Only moments naming this capability of the executable
              value_name: CAPABILITY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: names_command
              long: names-command
              positional: false
              help: Only moments naming this command
              value_name: NAMES_COMMAND
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: featured
              long: featured
              positional: false
              help: Only the moments the homepage features
              value_name: FEATURED
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: all
              long: all
              positional: false
              help: "Include drafts and deprecated moments, not only the public ones"
              value_name: ALL
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: query
              long: query
              short: q
              positional: false
              help: "Case-insensitive text over identities, titles, hooks, summaries, tags, aliases, signals, examples and bodies"
              value_name: QUERY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: why-show
              title: "One moment, with every relation derived from its metadata"
              description: "The record as its file declares it, then what nobody authored: the responsibilities its claims belong to, the moments that name it, and the moments nearest it by shared area, audience and tag."
              argv:
                - why
                - show
                - fixture-moment
              command: majordomus why show fixture-moment
              setup: []
              expectation: "exits 0; prints fixture-moment, derived"
          subcommands: []
        - path:
            - majordomus
            - why
            - audiences
          route: /docs/cli/why/audiences/
          executable: true
          usage: "majordomus why audiences [OPTIONS]"
          about: "Every audience, with the moments that name it"
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
            - name: audience
              long: audience
              positional: false
              help: Only moments this audience recognises
              value_name: AUDIENCE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: area
              long: area
              positional: false
              help: Only moments in this operational area
              value_name: AREA
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: tag
              long: tag
              positional: false
              help: Only moments carrying this tag
              value_name: TAG
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: severity
              long: severity
              positional: false
              help: Only moments of this severity
              value_name: SEVERITY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: frequency
              long: frequency
              positional: false
              help: Only moments of this frequency
              value_name: FREQUENCY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: lifecycle
              long: lifecycle
              positional: false
              help: Only moments at this stage of work
              value_name: LIFECYCLE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: capability
              long: capability
              positional: false
              help: Only moments naming this capability of the executable
              value_name: CAPABILITY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: names_command
              long: names-command
              positional: false
              help: Only moments naming this command
              value_name: NAMES_COMMAND
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: featured
              long: featured
              positional: false
              help: Only the moments the homepage features
              value_name: FEATURED
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: all
              long: all
              positional: false
              help: "Include drafts and deprecated moments, not only the public ones"
              value_name: ALL
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: query
              long: query
              short: q
              positional: false
              help: "Case-insensitive text over identities, titles, hooks, summaries, tags, aliases, signals, examples and bodies"
              value_name: QUERY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: why-audiences
              title: "Who recognises what, with the counts derived"
              description: "Each audience with how many public moments name it. The number is computed from the moments; an audience's own file never lists one."
              argv:
                - why
                - audiences
              command: majordomus why audiences
              setup: []
              expectation: "exits 0; prints SLUG, TITLE"
          subcommands: []
        - path:
            - majordomus
            - why
            - areas
          route: /docs/cli/why/areas/
          executable: true
          usage: "majordomus why areas [OPTIONS]"
          about: "Every operational area, with the moments that fall under it"
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
            - name: audience
              long: audience
              positional: false
              help: Only moments this audience recognises
              value_name: AUDIENCE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: area
              long: area
              positional: false
              help: Only moments in this operational area
              value_name: AREA
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: tag
              long: tag
              positional: false
              help: Only moments carrying this tag
              value_name: TAG
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: severity
              long: severity
              positional: false
              help: Only moments of this severity
              value_name: SEVERITY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: frequency
              long: frequency
              positional: false
              help: Only moments of this frequency
              value_name: FREQUENCY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: lifecycle
              long: lifecycle
              positional: false
              help: Only moments at this stage of work
              value_name: LIFECYCLE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: capability
              long: capability
              positional: false
              help: Only moments naming this capability of the executable
              value_name: CAPABILITY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: names_command
              long: names-command
              positional: false
              help: Only moments naming this command
              value_name: NAMES_COMMAND
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: featured
              long: featured
              positional: false
              help: Only the moments the homepage features
              value_name: FEATURED
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: all
              long: all
              positional: false
              help: "Include drafts and deprecated moments, not only the public ones"
              value_name: ALL
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: query
              long: query
              short: q
              positional: false
              help: "Case-insensitive text over identities, titles, hooks, summaries, tags, aliases, signals, examples and bodies"
              value_name: QUERY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: why-areas
              title: "The operational areas, with the counts derived"
              description: "The same relation read the other way: each area with the public moments that fall under it."
              argv:
                - why
                - areas
              command: majordomus why areas
              setup: []
              expectation: "exits 0; prints SLUG, TITLE"
          subcommands: []
        - path:
            - majordomus
            - why
            - diagnose
          route: /docs/cli/why/diagnose/
          executable: true
          usage: "majordomus why diagnose [OPTIONS]"
          about: "What the symptoms you recognise imply: the areas they weigh towards and the mechanisms that answer them"
          args:
            - name: signals
              long: signal
              positional: false
              help: "A signal id or a moment id; repeat for each one you recognise. Without any, the questionnaire is printed"
              value_name: SIGNALS
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
            - name: audience
              long: audience
              positional: false
              help: Only moments this audience recognises
              value_name: AUDIENCE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: area
              long: area
              positional: false
              help: Only moments in this operational area
              value_name: AREA
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: tag
              long: tag
              positional: false
              help: Only moments carrying this tag
              value_name: TAG
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: severity
              long: severity
              positional: false
              help: Only moments of this severity
              value_name: SEVERITY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: frequency
              long: frequency
              positional: false
              help: Only moments of this frequency
              value_name: FREQUENCY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: lifecycle
              long: lifecycle
              positional: false
              help: Only moments at this stage of work
              value_name: LIFECYCLE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: capability
              long: capability
              positional: false
              help: Only moments naming this capability of the executable
              value_name: CAPABILITY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: names_command
              long: names-command
              positional: false
              help: Only moments naming this command
              value_name: NAMES_COMMAND
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: featured
              long: featured
              positional: false
              help: Only the moments the homepage features
              value_name: FEATURED
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: all
              long: all
              positional: false
              help: "Include drafts and deprecated moments, not only the public ones"
              value_name: ALL
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: query
              long: query
              short: q
              positional: false
              help: "Case-insensitive text over identities, titles, hooks, summaries, tags, aliases, signals, examples and bodies"
              value_name: QUERY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: why-diagnose-questions
              title: "The questionnaire, assembled from the catalogue's own signals"
              description: "With no selection there is nothing to diagnose, so the questions are printed instead of an empty answer. Every line is a signal a moment declares; nothing here is a list of questions."
              argv:
                - why
                - diagnose
              command: majordomus why diagnose
              setup: []
              expectation: exits 0; prints Which of these happened to you this week?
            - id: why-diagnose
              title: What the symptoms you recognise imply
              description: "A name is a signal id or a moment id. The answer is counting, not inference: each recommendation carries the moments that produced it, and there is no percentage because there is no model behind one."
              argv:
                - why
                - diagnose
                - "--signal"
                - fixture-signal
              command: majordomus why diagnose --signal fixture-signal
              setup: []
              expectation: "exits 0; prints moment(s) matched, fixture-moment"
          subcommands: []
        - path:
            - majordomus
            - why
            - validate
          route: /docs/cli/why/validate/
          executable: true
          usage: "majordomus why validate [OPTIONS]"
          about: Every finding over the catalogue; exit 10 when any is an error
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
            - name: audience
              long: audience
              positional: false
              help: Only moments this audience recognises
              value_name: AUDIENCE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: area
              long: area
              positional: false
              help: Only moments in this operational area
              value_name: AREA
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: tag
              long: tag
              positional: false
              help: Only moments carrying this tag
              value_name: TAG
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: severity
              long: severity
              positional: false
              help: Only moments of this severity
              value_name: SEVERITY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: frequency
              long: frequency
              positional: false
              help: Only moments of this frequency
              value_name: FREQUENCY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: lifecycle
              long: lifecycle
              positional: false
              help: Only moments at this stage of work
              value_name: LIFECYCLE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: capability
              long: capability
              positional: false
              help: Only moments naming this capability of the executable
              value_name: CAPABILITY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: names_command
              long: names-command
              positional: false
              help: Only moments naming this command
              value_name: NAMES_COMMAND
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: featured
              long: featured
              positional: false
              help: Only the moments the homepage features
              value_name: FEATURED
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: all
              long: all
              positional: false
              help: "Include drafts and deprecated moments, not only the public ones"
              value_name: ALL
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: query
              long: query
              short: q
              positional: false
              help: "Case-insensitive text over identities, titles, hooks, summaries, tags, aliases, signals, examples and bodies"
              value_name: QUERY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: why-validate
              title: Check the catalogue before anything projects it
              description: "A reference that resolves to nothing, with the nearest candidate; a duplicate identity; a file name that disagrees with its id; a public record that does not meet the floor its status promises. Exit 10 on any error."
              argv:
                - why
                - validate
              command: majordomus why validate
              setup: []
              expectation: "exits 0; prints moment(s), valid"
          subcommands: []
    - path:
        - majordomus
        - devtask
      route: /docs/cli/devtask/
      executable: false
      usage: "majordomus devtask [OPTIONS] <COMMAND>"
      about: "One issue or one milestone as an executable development scope: what was authored, where the plan graph puts it, what happened to it, whether a worker may start it — with the origin of every value on the value"
      args:
        - name: repo
          long: repo
          positional: false
          help: "Start the search for the repository root here (default: the current directory)"
          value_name: PATH
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: discovery
          long: discovery
          positional: false
          help: How declarative files are enumerated
          value_name: DISCOVERY
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: vcs
              help: "Tracked files, through the version-control index (the layer's contract)"
            - name: filesystem
              help: A walk of the work tree with the same glob semantics; untracked files included
          defaults:
            - vcs
        - name: strict
          long: strict
          positional: false
          help: Refuse to proceed when any file of the layer carries an error diagnostic
          value_name: STRICT
          takes_value: false
          required: false
          global: true
          possible_values: []
          defaults:
            - "false"
        - name: share
          long: share
          positional: false
          help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
          value_name: DIR
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: format
          long: format
          positional: false
          help: Output shape
          value_name: FORMAT
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: text
              help: Lines for a person
            - name: json
              help: "One JSON document, deterministic"
          defaults:
            - text
      examples: []
      subcommands:
        - path:
            - majordomus
            - devtask
            - issue
          route: /docs/cli/devtask/issue/
          executable: true
          usage: "majordomus devtask issue [OPTIONS] <ID>"
          about: "One issue as an executable development task, every field carrying where it came from"
          args:
            - name: id
              positional: true
              help: "The issue id, as the canonical model spells it. An id the model does not declare is answered, not refused"
              value_name: ID
              takes_value: true
              required: true
              global: false
              possible_values: []
              defaults: []
            - name: no_git
              long: no-git
              positional: false
              help: "Answer from the canonical records alone, without consulting git — a deterministic answer that is the same on every machine"
              value_name: NO_GIT
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: devtask-issue
              title: "One issue as work to be done, not as metadata"
              description: "Identity, intent, status, milestone, dependencies, blockers, branches, commits, sessions and readiness in one answer, composed from the derivations that already own each half — the plan for the graph, `trace` for git, the canonical record for everything a person wrote. Nothing is manufactured: a key the record does not carry is `unknown` with the reason, never an empty string that reads as authored."
              argv:
                - devtask
                - issue
                - I0001
              command: majordomus devtask issue I0001
              setup: []
              expectation: "exits 0; prints readiness, explicit"
            - id: devtask-issue-json
              title: "The same, as the shape the API and MCP answer with"
              description: "One domain model behind every projection: this document is what `GET /api/v1/devtask/issue` returns and what the `majordomus_devtask` tool answers, with the four groups kept apart by the type — what a person authored, what the plan derives, what happened locally, and where the external projection stands — and a provenance on every field."
              argv:
                - devtask
                - issue
                - I0001
                - "--no-git"
                - "--format"
                - json
              command: majordomus devtask issue I0001 --no-git --format json
              setup: []
              expectation: "exits 0; prints one JSON document carrying /declaration/title/provenance, /position/status/provenance, /readiness/state, /attestation/explicit"
            - id: devtask-issue-undeclared
              title: "An id the model does not declare is answered, not refused"
              description: "A typo that read as \"nothing has been authored\" is the one answer a work surface must never give, so an unknown id answers with `declared: false`, the readiness `undeclared`, and every canonical field `unknown` with the reason on it."
              argv:
                - devtask
                - issue
                - I9999
                - "--no-git"
              command: majordomus devtask issue I9999 --no-git
              setup: []
              expectation: exits 0; prints undeclared
          subcommands: []
        - path:
            - majordomus
            - devtask
            - milestone
          route: /docs/cli/devtask/milestone/
          executable: true
          usage: "majordomus devtask milestone [OPTIONS] <ID>"
          about: "One milestone as an executable dependency graph: ready, blocked, parallelizable, critical blockers, cycles"
          args:
            - name: id
              positional: true
              help: "The milestone id, as the canonical model spells it"
              value_name: ID
              takes_value: true
              required: true
              global: false
              possible_values: []
              defaults: []
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: devtask-milestone
              title: "What to work on next in one outcome, and what to unblock first"
              description: "The issues partitioned by readiness, the critical blockers ordered by how much unfinished work each holds back, and the startable work partitioned into subsets that may genuinely run at the same time. A pure function of the canonical records — no git, no clock, no network — so two runs on two machines produce the same bytes and a reader derives nothing itself."
              argv:
                - devtask
                - milestone
                - foundation
              command: majordomus devtask milestone foundation
              setup: []
              expectation: exits 0; prints READINESS
            - id: devtask-milestone-json
              title: "The graph, as the shape the API and MCP answer with"
              description: "Every partition the plan implies and no surface should recompute: `ready`, `blocked`, `waiting`, `active`, `review`, `completion_blocked`, `complete`, `cancelled`, plus `critical_blockers`, `parallelizable` with the scope path behind each serialisation, and `cycles` as strongly connected components."
              argv:
                - devtask
                - milestone
                - foundation
                - "--format"
                - json
              command: majordomus devtask milestone foundation --format json
              setup: []
              expectation: "exits 0; prints one JSON document carrying /counts/total, /ready, /critical_blockers, /parallelizable"
          subcommands: []
    - path:
        - majordomus
        - distribution
      route: /docs/cli/distribution/
      executable: true
      usage: "majordomus distribution [OPTIONS] [COMMAND]"
      about: "How this project is packaged, published and installed: the platforms, the artifact names, the installer, the releases"
      args:
        - name: repo
          long: repo
          positional: false
          help: "Start the search for the repository root here (default: the current directory)"
          value_name: PATH
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: discovery
          long: discovery
          positional: false
          help: How declarative files are enumerated
          value_name: DISCOVERY
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: vcs
              help: "Tracked files, through the version-control index (the layer's contract)"
            - name: filesystem
              help: A walk of the work tree with the same glob semantics; untracked files included
          defaults:
            - vcs
        - name: strict
          long: strict
          positional: false
          help: Refuse to proceed when any file of the layer carries an error diagnostic
          value_name: STRICT
          takes_value: false
          required: false
          global: true
          possible_values: []
          defaults:
            - "false"
        - name: share
          long: share
          positional: false
          help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
          value_name: DIR
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: format
          long: format
          positional: false
          help: Output shape
          value_name: FORMAT
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: text
              help: Lines for a person
            - name: json
              help: "One JSON document, deterministic"
          defaults:
            - text
      examples:
        - id: distribution-show-default
          title: How this project is installed
          description: "`distribution` with nothing after it shows the model: the one-line install command, where an installation goes, and how many platforms a release builds. Every value comes from share/distribution.yaml, which is the only place any of them is written."
          argv:
            - distribution
          command: majordomus distribution
          setup: []
          expectation: "exits 0; prints binary, install, targets"
      subcommands:
        - path:
            - majordomus
            - distribution
            - show
          route: /docs/cli/distribution/show/
          executable: true
          usage: "majordomus distribution show [OPTIONS]"
          about: "The model: the install command, where an installation goes, and every declared target"
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: distribution-show-json
              title: The distribution model as one JSON document
              description: "The same answer as a document a script can read: the install command, the default locations, and every declared target with the artifact name the naming function derives for it. This is what the website's install block and the cockpit's install card render."
              argv:
                - distribution
                - show
                - "--format"
                - json
              command: majordomus distribution show --format json
              setup: []
              expectation: "exits 0; prints one JSON document carrying /install_command, /targets"
          subcommands: []
        - path:
            - majordomus
            - distribution
            - status
          route: /docs/cli/distribution/status/
          executable: true
          usage: "majordomus distribution status [OPTIONS]"
          about: "Whether the advertised one-line installation works right now, and what is missing when it does not"
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: distribution-status
              title: Whether the published one-line installation works right now
              description: "The operator's question — *can a machine that has never seen this project install it with the advertised command?* — answered from the distribution model and the release records, without touching the network. Each check names what was observed; a failing one names its cause and the command that changes it. Shown here in a repository that has published nothing, where the answer is no and the exit code is 10, which is what makes it usable as a check rather than as prose. `distribution validate` is the gate over the model itself; this is the gate over the state a user meets."
              argv:
                - distribution
                - status
              command: majordomus distribution status
              setup: []
              expectation: exits 10
          subcommands: []
        - path:
            - majordomus
            - distribution
            - validate
          route: /docs/cli/distribution/validate/
          executable: true
          usage: "majordomus distribution validate [OPTIONS]"
          about: Every invariant of the model and of the release records; exit 10 with each violation named
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: distribution-validate
              title: Every invariant of the model and of the release records
              description: "Refuses a duplicate target id or triple, two targets deriving one artifact name, a Linux target with no C library, a published target with nothing to build it on, an unbuilt target with no recorded reason, a base URL that is not HTTPS, and a release record that misses a supported target, renames an artifact or serves one from another host. Exits 10 with each violation named."
              argv:
                - distribution
                - validate
              command: majordomus distribution validate
              setup: []
              expectation: exits 0; prints distribution
          subcommands: []
        - path:
            - majordomus
            - distribution
            - targets
          route: /docs/cli/distribution/targets/
          executable: true
          usage: "majordomus distribution targets [OPTIONS]"
          about: "Every declared target, one line each, with the artifact name it derives"
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: distribution-targets
              title: "Every platform, and whether a release builds it"
              description: "One line per declared target: its id, its Rust target triple, whether it is supported, experimental or unavailable, and how it is written in prose. The supported-platform table in the documentation and the installer's own refusal message are rendered from these same rows."
              argv:
                - distribution
                - targets
              command: majordomus distribution targets
              setup: []
              expectation: "exits 0; prints RUST TARGET, supported"
          subcommands: []
        - path:
            - majordomus
            - distribution
            - matrix
          route: /docs/cli/distribution/matrix/
          executable: true
          usage: "majordomus distribution matrix [OPTIONS]"
          about: "The release build matrix, as the release workflow reads it"
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: distribution-matrix
              title: The release build matrix the workflow runs
              description: "One entry per published target, with the runner it is built on, the packages that runner needs, and the artifact name with `{tag}` where a release's tag goes. The release workflow reads this and states no platform of its own; adding a target to the model adds a build here and nowhere else."
              argv:
                - distribution
                - matrix
              command: majordomus distribution matrix
              setup: []
              expectation: "exits 0; prints one JSON document carrying /include, /binary"
          subcommands: []
        - path:
            - majordomus
            - distribution
            - artifact
          route: /docs/cli/distribution/artifact/
          executable: true
          usage: "majordomus distribution artifact [OPTIONS]"
          about: The archive name and root directory a target and a tag derive
          args:
            - name: target
              long: target
              positional: false
              help: "A target's id or its Rust target triple"
              value_name: TARGET
              takes_value: true
              required: true
              global: false
              possible_values: []
              defaults: []
            - name: tag
              long: tag
              positional: false
              help: "The tag, `v` and a version"
              value_name: TAG
              takes_value: true
              required: true
              global: false
              possible_values: []
              defaults: []
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: distribution-artifact
              title: What one target and one tag are called
              description: "The one naming function, asked directly: the archive's name, the directory it unpacks into, and where a release publishes it. `scripts/release-package` asks this rather than composing a name, so a change to the naming function reaches the packaging without an edit."
              argv:
                - distribution
                - artifact
                - "--target"
                - aarch64-apple-darwin
                - "--tag"
                - v0.2.0
                - "--format"
                - json
              command: majordomus distribution artifact --target aarch64-apple-darwin --tag v0.2.0 --format json
              setup: []
              expectation: "exits 0; prints one JSON document carrying /name, /root, /url"
          subcommands: []
        - path:
            - majordomus
            - distribution
            - releases
          route: /docs/cli/distribution/releases/
          executable: true
          usage: "majordomus distribution releases [OPTIONS]"
          about: "Every recorded release, newest first, and the one an unpinned installation resolves to"
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: distribution-releases
              title: "What has been published, and what an unpinned install resolves to"
              description: "Every release record this repository holds, newest first, and which of them the stable pointer names: the highest version among the stable, unwithdrawn records. The pointer is derived on every read and is authored nowhere."
              argv:
                - distribution
                - releases
              command: majordomus distribution releases
              setup: []
              expectation: exits 0
          subcommands: []
        - path:
            - majordomus
            - distribution
            - metadata
          route: /docs/cli/distribution/metadata/
          executable: true
          usage: "majordomus distribution metadata [OPTIONS]"
          about: "The public metadata one release record publishes, rendered from the record alone"
          args:
            - name: record
              long: record
              positional: false
              help: A release record; the file the release pipeline writes under .ai/repo/releases/
              value_name: FILE
              takes_value: true
              required: true
              global: false
              possible_values: []
              defaults: []
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: distribution-metadata
              title: What one release record publishes
              description: "The public metadata a record turns into, rendered from the record and the model alone: the release pipeline prints this before it commits anything, and the installer's own tests serve it as a release that never existed. Shown here in a repository that has published nothing, where the record does not exist and the command says which file it wanted and exits 10 rather than inventing one."
              argv:
                - distribution
                - metadata
                - "--record"
                - .ai/repo/releases/v0.2.0.yaml
              command: majordomus distribution metadata --record .ai/repo/releases/v0.2.0.yaml
              setup: []
              expectation: exits 10
          subcommands: []
        - path:
            - majordomus
            - distribution
            - build
          route: /docs/cli/distribution/build/
          executable: true
          usage: "majordomus distribution build [OPTIONS]"
          about: "What this executable is: version, target triple, profile, commit"
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: distribution-build
              title: What this executable is
              description: "The crate version, the Rust target triple, the profile and the commit, all compiled in at build time. An installed binary answers this without a repository, a toolchain or git, which is what makes a support question answerable."
              argv:
                - distribution
                - build
              command: majordomus distribution build
              setup: []
              expectation: "exits 0; prints version, target, commit"
          subcommands: []
    - path:
        - majordomus
        - env
      route: /docs/cli/env/
      executable: true
      usage: "majordomus env [OPTIONS] [COMMAND]"
      about: "What this checkout is: the project, version control, the toolchains it declares, what the layer holds, the workflows, the provider projections and the local services"
      args:
        - name: repo
          long: repo
          positional: false
          help: "Start the search for the repository root here (default: the current directory)"
          value_name: PATH
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: discovery
          long: discovery
          positional: false
          help: How declarative files are enumerated
          value_name: DISCOVERY
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: vcs
              help: "Tracked files, through the version-control index (the layer's contract)"
            - name: filesystem
              help: A walk of the work tree with the same glob semantics; untracked files included
          defaults:
            - vcs
        - name: strict
          long: strict
          positional: false
          help: Refuse to proceed when any file of the layer carries an error diagnostic
          value_name: STRICT
          takes_value: false
          required: false
          global: true
          possible_values: []
          defaults:
            - "false"
        - name: share
          long: share
          positional: false
          help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
          value_name: DIR
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: format
          long: format
          positional: false
          help: Output shape
          value_name: FORMAT
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: text
              help: Lines for a person
            - name: json
              help: "One JSON document, deterministic"
          defaults:
            - text
      examples:
        - id: env-status
          title: What this checkout is
          description: "`env` with nothing after it resolves the whole snapshot: the project and its version, the repository and its layer, version control, the toolchains the repository declares, what the layer holds counted per kind, the workflows the runner describes, the provider projections against the policy that renders them, and the local services. This is the resolution that counts the layer, so it builds the index and writes the cache the banner reads."
          argv:
            - env
          command: majordomus env
          setup: []
          expectation: "exits 0; prints project, repository, resolution"
      subcommands:
        - path:
            - majordomus
            - env
            - status
          route: /docs/cli/env/status/
          executable: true
          usage: "majordomus env status [OPTIONS]"
          about: "The whole snapshot, resolved in full: what the layer holds is counted, and the cache the banner reads is written"
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: env-status-json
              title: The snapshot as one document
              description: "The same value the HTTP route `/api/v1/environment` and the MCP resource `majordomus://environment` answer with, and the value the banner renders. Every field carries where it came from under `provenance`, and a value nothing could resolve is absent rather than zero."
              argv:
                - env
                - status
                - "--format"
                - json
              command: majordomus env status --format json
              setup: []
              expectation: "exits 0; prints one JSON document carrying /schema, /project/version, /repository/name, /provenance"
          subcommands: []
        - path:
            - majordomus
            - env
            - banner
          route: /docs/cli/env/banner/
          executable: true
          usage: "majordomus env banner [OPTIONS]"
          about: "Render the snapshot for a terminal. Goes to standard error, never standard output, because direnv reads standard output as the environment it is setting"
          args:
            - name: mode
              long: mode
              positional: false
              help: "How much to show: `auto`, `full`, `compact` or `off`. Without it, MAJORDOMUS_BANNER decides, and without that, `auto` — which is silent when nothing is watching, shows the whole box when the repository has something new to say, and the two-line form when it does not"
              value_name: MODE
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: width
              long: width
              positional: false
              help: "Draw as if the terminal were this wide, whatever it is"
              value_name: COLUMNS
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: env-banner-compact
              title: "The two-line form, at a width you choose"
              description: "What `direnv` renders on entering the repository. It resolves fast — it never builds the index — and it writes to standard error, because direnv reads the standard output of a `.envrc` as the environment it is applying. `--width` renders as if the terminal were that wide, which is what makes the layout testable."
              argv:
                - env
                - banner
                - "--mode"
                - compact
                - "--width"
                - "80"
              command: majordomus env banner --mode compact --width 80
              setup: []
              expectation: exits 0
          subcommands: []
        - path:
            - majordomus
            - env
            - export
          route: /docs/cli/env/export/
          executable: true
          usage: "majordomus env export [OPTIONS]"
          about: "The variable assignments a shell in this repository benefits from, for `eval`. Assignments only: no command, no side effect"
          args:
            - name: shell
              long: shell
              positional: false
              help: "The shell to write for: `direnv`, `bash`, `zsh`, `sh`, `ksh` or `fish`"
              value_name: SHELL
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults:
                - direnv
            - name: banner
              long: banner
              positional: false
              help: "Also draw the banner, to standard error, from the same snapshot. What an adapter asks for: one process on the path a shell takes on every entry, rather than two that each pay for a `git status`"
              value_name: BANNER
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: mode
              long: mode
              positional: false
              help: "With --banner, how much to show; MAJORDOMUS_BANNER decides without it"
              value_name: MODE
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: bridge
              long: bridge
              positional: false
              help: "Also refresh the workflow bridge under .ai/local/cache/ when a declaration behind it has changed. A few `stat` calls when nothing has; never a build, never a network call"
              value_name: BRIDGE
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: env-export-direnv
              title: The assignments a shell in this repository wants
              description: "Assignments and nothing else, safe to `eval`: no command runs, no file is touched, and every value is quoted so that a repository path holding a quote or a `$(...)` cannot become shell code. This is the whole of what `.envrc` needs from Majordomus."
              argv:
                - env
                - export
                - "--shell"
                - direnv
              command: majordomus env export --shell direnv
              setup: []
              expectation: exits 0; prints export MAJORDOMUS_ROOT=
          subcommands: []
        - path:
            - majordomus
            - env
            - enter
          route: /docs/cli/env/enter/
          executable: true
          usage: "majordomus env enter [OPTIONS]"
          about: "Enter the repository: the assignments a shell here benefits from on standard output, the banner on standard error, the workflow bridge refreshed when a declaration behind it moved, and the runtime ensured — the whole of what entering this repository is, as one call, so that no person and no agent has to remember a sequence. Never builds, never reaches a remote network, and never waits for a server it started to answer"
          args:
            - name: shell
              long: shell
              positional: false
              help: "The shell to write for: `direnv`, `bash`, `zsh`, `sh`, `ksh` or `fish`"
              value_name: SHELL
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults:
                - direnv
            - name: mode
              long: mode
              positional: false
              help: How much banner to draw; MAJORDOMUS_BANNER decides without it
              value_name: MODE
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: no_banner
              long: no-banner
              positional: false
              help: Do not draw the banner
              value_name: NO_BANNER
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: no_bridge
              long: no-bridge
              positional: false
              help: Do not refresh the workflow bridge
              value_name: NO_BRIDGE
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: no_runtime
              long: no-runtime
              positional: false
              help: "Do not ensure the runtime: export, draw and refresh only. What MAJORDOMUS_RUNTIME=off says, as an argument"
              value_name: NO_RUNTIME
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: wait
              long: wait
              positional: false
              help: "Wait this many seconds for a server this call started to answer. Zero — the default, and what a shell prompt asks for — returns as soon as one has been started, and the entry file's watch over the lease brings the address in when it is published"
              value_name: SECONDS
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults:
                - "0"
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: env-enter
              title: "Everything entering this repository is, as one call"
              description: "What the file a shell evaluates on entry runs, and the only call it makes (ADR 0043): the assignments on standard output for `eval`, the banner on standard error, the workflow bridge refreshed when a declaration behind it moved, and the repository's shared server ensured when nothing is serving this checkout. It never builds, never ensures from an executable older than its sources, never waits for a server it started to answer, and never exits non-zero — a non-zero exit here would make direnv report that the whole environment failed. `--no-runtime` is what this example passes, because an example is not the place to start a server."
              argv:
                - env
                - enter
                - "--shell"
                - direnv
                - "--no-banner"
                - "--no-bridge"
                - "--no-runtime"
              command: majordomus env enter --shell direnv --no-banner --no-bridge --no-runtime
              setup: []
              expectation: exits 0; prints export MAJORDOMUS_ROOT=
          subcommands: []
        - path:
            - majordomus
            - env
            - explain
          route: /docs/cli/env/explain/
          executable: true
          usage: "majordomus env explain [OPTIONS] [FIELD]"
          about: "Where each value came from: the file, command or constant that decided it, the resolver that read it, and how far it can be trusted"
          args:
            - name: field
              positional: true
              help: "One field in dotted form (`vcs.branch`, `layer.objects`), or a prefix; every field when absent"
              value_name: FIELD
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: env-explain-field
              title: Where one value came from
              description: "An inferred system without provenance is magic. Every field of the snapshot can name the file, command or compile-time constant that decided it, the resolver that read it, and whether it was read now, taken from the cache, or not resolved at all."
              argv:
                - env
                - explain
                - project.version
              command: majordomus env explain project.version
              setup: []
              expectation: "exits 0; prints project.version, source, resolver"
          subcommands: []
    - path:
        - majordomus
        - commands
      route: /docs/cli/commands/
      executable: true
      usage: "majordomus commands [OPTIONS] [COMMAND]"
      about: "Every command this repository offers, from whichever program offers it: the graph, one command, where each one is projected, and the workflow bridge derived from it"
      args:
        - name: repo
          long: repo
          positional: false
          help: "Start the search for the repository root here (default: the current directory)"
          value_name: PATH
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: discovery
          long: discovery
          positional: false
          help: How declarative files are enumerated
          value_name: DISCOVERY
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: vcs
              help: "Tracked files, through the version-control index (the layer's contract)"
            - name: filesystem
              help: A walk of the work tree with the same glob semantics; untracked files included
          defaults:
            - vcs
        - name: strict
          long: strict
          positional: false
          help: Refuse to proceed when any file of the layer carries an error diagnostic
          value_name: STRICT
          takes_value: false
          required: false
          global: true
          possible_values: []
          defaults:
            - "false"
        - name: share
          long: share
          positional: false
          help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
          value_name: DIR
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: format
          long: format
          positional: false
          help: Output shape
          value_name: FORMAT
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: text
              help: Lines for a person
            - name: json
              help: "One JSON document, deterministic"
          defaults:
            - text
      examples:
        - id: commands-list
          title: Every command this repository offers
          description: "The command graph, composed from the three declarations that already exist: the clap tree of this executable, the shipped command registry of the shell tool, and the recipes the workflow runner describes. One line per command, with the program that runs it and what running it changes."
          argv:
            - commands
          command: majordomus commands
          setup: []
          expectation: "exits 0; prints commands, executable, read-only"
      subcommands:
        - path:
            - majordomus
            - commands
            - list
          route: /docs/cli/commands/list/
          executable: true
          usage: "majordomus commands list [OPTIONS]"
          about: "Every command, one line each: what it is, what running it changes, and where it is projected"
          args:
            - name: origin
              long: origin
              positional: false
              help: Only the commands of this program
              value_name: ORIGIN
              takes_value: true
              required: false
              global: false
              possible_values:
                - name: executable
                  help: This executable
                - name: tool
                  help: "The shell tool, bin/majordomus"
                - name: workflow
                  help: A workflow the repository declares
              defaults: []
            - name: effect
              long: effect
              positional: false
              help: Only the commands whose effect is at most this
              value_name: EFFECT
              takes_value: true
              required: false
              global: false
              possible_values:
                - name: read-only
                  help: Reads and answers
                - name: local-mutation
                  help: Writes only what no commit carries
                - name: repository-mutation
                  help: Writes tracked files
                - name: network-mutation
                  help: Reaches the network with an effect
                - name: destructive
                  help: Removes something
              defaults: []
            - name: search
              long: search
              positional: false
              help: "Only the commands matching this text, in their invocation, summary, tags or identity"
              value_name: TEXT
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: commands-list-filtered
              title: Only what reads
              description: "The filters are the graph's own vocabulary rather than a search over text: `--effect read-only` is every command that changes nothing anywhere, which is the same predicate the exposure policy uses to decide what a machine surface may call."
              argv:
                - commands
                - list
                - "--effect"
                - read-only
              command: majordomus commands list --effect read-only
              setup: []
              expectation: exits 0; prints read-only
          subcommands: []
        - path:
            - majordomus
            - commands
            - show
          route: /docs/cli/commands/show/
          executable: true
          usage: "majordomus commands show [OPTIONS] <ID>"
          about: "One command in full: its arguments, its effect, what it needs, and every surface that carries it"
          args:
            - name: id
              positional: true
              help: "The command's identity, `executable.worktree.status`"
              value_name: ID
              takes_value: true
              required: true
              global: false
              possible_values: []
              defaults: []
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: commands-show
              title: "One command, and every surface that carries it"
              description: "The arguments with the source of each one's values, the effect, and the projections: the command line, the workflow recipe, the MCP tool, the HTTP route, the Cockpit and the page. A surface that withholds it says why."
              argv:
                - commands
                - show
                - executable.worktree.status
              command: majordomus commands show executable.worktree.status
              setup: []
              expectation: "exits 0; prints executable.worktree.status, projections"
          subcommands: []
        - path:
            - majordomus
            - commands
            - explain
          route: /docs/cli/commands/explain/
          executable: true
          usage: "majordomus commands explain [OPTIONS] <ID>"
          about: "Why one command appears where it does: the declaration it came from, the policy that placed it, and the reason for every surface that withholds it"
          args:
            - name: id
              positional: true
              help: "The command's identity, `executable.worktree.status`"
              value_name: ID
              takes_value: true
              required: true
              global: false
              possible_values: []
              defaults: []
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: commands-explain
              title: Why a command appears where it does
              description: "The same command with its provenance: the file that declares it, the reader that found it, the capability behind it when there is one, what it requires, and the file the exposure policy lives in. Nothing about a command's placement is a mystery a grep has to solve."
              argv:
                - commands
                - explain
                - executable.serve
              command: majordomus commands explain executable.serve
              setup: []
              expectation: "exits 0; prints declared in, policy"
          subcommands: []
        - path:
            - majordomus
            - commands
            - graph
          route: /docs/cli/commands/graph/
          executable: true
          usage: "majordomus commands graph [OPTIONS]"
          about: "The whole graph as one document, with its fingerprint and every diagnostic"
          args:
            - name: check
              long: check
              positional: false
              help: Exit 10 when the graph carries an error
              value_name: CHECK
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: commands-graph-json
              title: The whole graph as one document
              description: "Deterministic and fingerprinted: two builds over one tree produce the same bytes, which is what lets the workflow bridge, the completion index and the Cockpit all key on the fingerprint instead of regenerating."
              argv:
                - commands
                - graph
                - "--format"
                - json
              command: majordomus commands graph --format json
              setup: []
              expectation: "exits 0; prints one JSON document carrying /schema, /fingerprint, /commands"
          subcommands: []
        - path:
            - majordomus
            - commands
            - bridge
          route: /docs/cli/commands/bridge/
          executable: true
          usage: "majordomus commands bridge [OPTIONS]"
          about: "Materialise the workflow bridge from the graph, and refresh the cache the completion reads; writes nothing when the graph has not changed"
          args:
            - name: check
              long: check
              positional: false
              help: Exit 10 when the materialised bridge is not the one this graph projects; write nothing
              value_name: CHECK
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: commands-bridge
              title: "The workflow runner's recipes, derived"
              description: "Every command of both programs, written as a recipe that runs the canonical program with the caller's own arguments. It goes under .ai/local/cache/, which no commit carries, and it is rewritten only when the graph's fingerprint changes."
              argv:
                - commands
                - bridge
              command: majordomus commands bridge
              setup: []
              expectation: "exits 0; prints bridge, recipe"
          subcommands: []
    - path:
        - majordomus
        - completion
      route: /docs/cli/completion/
      executable: true
      usage: "majordomus completion [OPTIONS] [COMMAND]"
      about: "Completion for any surface, answered from the command graph: the candidates a shell asks for, and the one-time integration that asks"
      args:
        - name: repo
          long: repo
          positional: false
          help: "Start the search for the repository root here (default: the current directory)"
          value_name: PATH
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: discovery
          long: discovery
          positional: false
          help: How declarative files are enumerated
          value_name: DISCOVERY
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: vcs
              help: "Tracked files, through the version-control index (the layer's contract)"
            - name: filesystem
              help: A walk of the work tree with the same glob semantics; untracked files included
          defaults:
            - vcs
        - name: strict
          long: strict
          positional: false
          help: Refuse to proceed when any file of the layer carries an error diagnostic
          value_name: STRICT
          takes_value: false
          required: false
          global: true
          possible_values: []
          defaults:
            - "false"
        - name: share
          long: share
          positional: false
          help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
          value_name: DIR
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
      examples:
        - id: completion-default
          title: The integration a person installs once
          description: "With no subcommand, the shell integration for zsh. It contains no command, no flag and no identifier: every candidate comes from a query against the command graph of the repository the shell is in, so one integration serves every checkout and never goes stale."
          argv:
            - completion
          command: majordomus completion
          setup: []
          expectation: "exits 0; prints completion query, compdef"
      subcommands:
        - path:
            - majordomus
            - completion
            - query
          route: /docs/cli/completion/query/
          executable: true
          usage: "majordomus completion query [OPTIONS] [WORD]"
          about: "The candidates for one command line, from the command graph. What a shell adapter calls on every TAB"
          args:
            - name: surface
              long: surface
              positional: false
              help: Which surface the words are spelled for
              value_name: SURFACE
              takes_value: true
              required: false
              global: false
              possible_values:
                - name: cli
                  help: The command line of either program
                - name: workflow
                  help: The workflow runner
              defaults:
                - cli
            - name: cursor
              long: cursor
              positional: false
              help: The index of the word the cursor is in; the default is a new word after the last
              value_name: N
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: false
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
            - name: words
              positional: true
              help: "The words of the command line, the program's own name first"
              value_name: WORD
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: completion-query
              title: What a shell asks on every TAB
              description: "The words of the command line and the position of the cursor; back come the candidates with their descriptions. The same call answers the workflow runner's completion with `--surface workflow`, resolving the recipe name to the command it bridges and then completing that command's own arguments."
              argv:
                - completion
                - query
                - "--surface"
                - cli
                - "--"
                - majordomus
                - work
              command: majordomus completion query --surface cli -- majordomus work
              setup: []
              expectation: exits 0; prints worktree
          subcommands: []
        - path:
            - majordomus
            - completion
            - init
          route: /docs/cli/completion/init/
          executable: true
          usage: "majordomus completion init [OPTIONS]"
          about: "The shell integration to load once, which carries no command of its own and asks this executable for every candidate"
          args:
            - name: shell
              long: shell
              positional: false
              help: Which shell to print the integration for
              value_name: SHELL
              takes_value: true
              required: false
              global: false
              possible_values:
                - name: zsh
                  help: zsh
                - name: bash
                  help: bash
                - name: fish
                  help: fish
              defaults:
                - zsh
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: completion-init-bash
              title: "The same, for bash"
              description: "A different shell's protocol, the same question. Both adapters read the words being completed, find the cursor, ask this executable and print what comes back."
              argv:
                - completion
                - init
                - "--shell"
                - bash
              command: majordomus completion init --shell bash
              setup: []
              expectation: "exits 0; prints completion query, complete -F"
          subcommands: []
        - path:
            - majordomus
            - completion
            - install
          route: /docs/cli/completion/install/
          executable: true
          usage: "majordomus completion install [OPTIONS]"
          about: "Put that integration into the shell's startup file, between managed markers, so that no one maintains it by hand"
          args:
            - name: shell
              long: shell
              positional: false
              help: Which shell to install for; decides the startup file when --rc is not given
              value_name: SHELL
              takes_value: true
              required: false
              global: false
              possible_values:
                - name: zsh
                  help: zsh
                - name: bash
                  help: bash
                - name: fish
                  help: fish
              defaults:
                - zsh
            - name: rc
              long: rc
              positional: false
              help: "The startup file to write, instead of the shell's usual one"
              value_name: PATH
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: remove
              long: remove
              positional: false
              help: "Take the block out again, leaving the rest of the file as it was"
              value_name: REMOVE
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: dry_run
              long: dry-run
              positional: false
              help: Say what would change and write nothing
              value_name: DRY_RUN
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: completion-install-dry-run
              title: "The one line a person adds to their shell, added for them"
              description: "Writes the integration into the shell's startup file between `# >>> MAJORDOMUS >>>` markers: nothing outside them is touched, running it twice changes nothing, and `--remove` takes it out again. It is never a side effect of anything else — installing into a person's home directory is its own decision, so it is its own command. `--dry-run` says what would change and writes nothing."
              argv:
                - completion
                - install
                - "--shell"
                - zsh
                - "--dry-run"
              command: majordomus completion install --shell zsh --dry-run
              setup: []
              expectation: exits 0
          subcommands: []
    - path:
        - majordomus
        - worktree
      route: /docs/cli/worktree/
      executable: true
      usage: "majordomus worktree [OPTIONS] [COMMAND]"
      about: "The branch-to-worktree topology: where every linked worktree belongs (`<repo>-wt/<branch>`), where each one is, and the lifecycle — create, migrate, repair, guard"
      args:
        - name: repo
          long: repo
          positional: false
          help: "Start the search for the repository root here (default: the current directory)"
          value_name: PATH
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: discovery
          long: discovery
          positional: false
          help: How declarative files are enumerated
          value_name: DISCOVERY
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: vcs
              help: "Tracked files, through the version-control index (the layer's contract)"
            - name: filesystem
              help: A walk of the work tree with the same glob semantics; untracked files included
          defaults:
            - vcs
        - name: strict
          long: strict
          positional: false
          help: Refuse to proceed when any file of the layer carries an error diagnostic
          value_name: STRICT
          takes_value: false
          required: false
          global: true
          possible_values: []
          defaults:
            - "false"
        - name: share
          long: share
          positional: false
          help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
          value_name: DIR
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: format
          long: format
          positional: false
          help: Output shape
          value_name: FORMAT
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: text
              help: Lines for a person
            - name: json
              help: "One JSON document, deterministic"
          defaults:
            - text
      examples:
        - id: worktree-default-status
          title: "Where am I, and is that where I belong?"
          description: "`worktree` with nothing after it answers the question a worker asks before starting: which branch this is, whether this directory is that branch's canonical worktree (or the primary checkout on the trunk), what is uncommitted here, and how many errors the whole topology carries. The same answer from the primary checkout and from four directories deep inside a linked worktree."
          argv:
            - worktree
          command: majordomus worktree
          setup: []
          expectation: "exits 0; prints branch, worktree, container"
      aliases:
        - wt
      subcommands:
        - path:
            - majordomus
            - worktree
            - status
          route: /docs/cli/worktree/status/
          executable: true
          usage: "majordomus worktree status [OPTIONS]"
          about: "Where this call is — branch, worktree, canonical or not, uncommitted work — and how many errors the whole topology carries; exit 10 when this worktree is out of place"
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: worktree-status-json
              title: The current worktree as one document
              description: "The same answer as JSON: the repository, the container, the trunk and how it was decided, this worktree with its standing and diagnostics, and whether it is where it belongs. This is what the MCP tool `majordomus_worktree_status` and `GET /api/v1/worktrees/status` answer."
              argv:
                - worktree
                - status
                - "--format"
                - json
              command: majordomus worktree status --format json
              setup: []
              expectation: "exits 0; prints one JSON document carrying /worktree/standing, /container/path, /trunk/source, /canonical"
          subcommands: []
        - path:
            - majordomus
            - worktree
            - list
          route: /docs/cli/worktree/list/
          executable: true
          usage: "majordomus worktree list [OPTIONS]"
          about: "Every registered worktree with its standing, one line each; exit 10 when the topology has an error"
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: worktree-list-text
              title: "Every worktree, the misplaced ones obvious"
              description: "The primary checkout first, then every linked worktree with its standing, its branch, where it belongs when it is somewhere else, and its uncommitted work. The primary checkout is exempt from the path rule and held to the trunk rule instead."
              argv:
                - worktree
                - list
              command: majordomus worktree list
              setup:
                - argv:
                    - worktree
                    - create
                    - feature/example
                  command: majordomus worktree create feature/example
              expectation: "exits 0; prints PRIMARY, CANONICAL, -wt/feature/example"
          subcommands: []
        - path:
            - majordomus
            - worktree
            - topology
          route: /docs/cli/worktree/topology/
          executable: true
          usage: "majordomus worktree topology [OPTIONS]"
          about: "The whole topology: repository, container, trunk, every worktree, every branch without a worktree, every diagnostic; exit 10 when it has an error"
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: worktree-topology-json
              title: The whole topology as one document
              description: "The repository, the container, the trunk, every worktree, every branch with or without a worktree, every diagnostic with its code and remedy, and the tallies. This is what the MCP resource `majordomus://worktrees`, the tool `majordomus_worktrees`, `GET /api/v1/worktrees` and the Cockpit all render."
              argv:
                - worktree
                - topology
                - "--format"
                - json
              command: majordomus worktree topology --format json
              setup: []
              expectation: "exits 0; prints one JSON document carrying /container/path, /trunk/branch, /worktrees/0/standing, /branches/0/name, /tallies/worktrees, /valid"
          subcommands: []
        - path:
            - majordomus
            - worktree
            - root
          route: /docs/cli/worktree/root/
          executable: true
          usage: "majordomus worktree root [OPTIONS]"
          about: "Print the container every linked worktree belongs under, and nothing else: `cd \"$(majordomus worktree root)\"`"
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: worktree-root-path
              title: "The container, for the shell"
              description: "Prints the container and nothing else, so a shell can use it: `cd \"$(majordomus worktree root)\"`. It is the primary checkout's sibling named with `-wt`, derived from git's own identity and never from the current directory — which is why running this inside a linked worktree does not answer a container inside that worktree."
              argv:
                - worktree
                - root
              command: majordomus worktree root
              setup: []
              expectation: exits 0; prints -wt
          subcommands: []
        - path:
            - majordomus
            - worktree
            - path
          route: /docs/cli/worktree/path/
          executable: true
          usage: "majordomus worktree path [OPTIONS] <BRANCH>"
          about: "Print the canonical path of a branch, and nothing else: `cd \"$(majordomus worktree path feature/x)\"`. Derived from the name; the branch need not exist"
          args:
            - name: branch
              positional: true
              help: "The branch, full name"
              value_name: BRANCH
              takes_value: true
              required: true
              global: false
              possible_values: []
              defaults: []
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: worktree-path-branch
              title: "The canonical path of a branch, for the shell"
              description: "A child process cannot change its parent shell's directory, so nothing here pretends to: this prints one path and the shell does the rest — `cd \"$(majordomus worktree path feature/x)\"`. The path is the branch name under the container, hierarchy kept; the branch need not exist yet."
              argv:
                - worktree
                - path
                - feature/providers/streaming
              command: majordomus worktree path feature/providers/streaming
              setup: []
              expectation: exits 0; prints -wt/feature/providers/streaming
          subcommands: []
        - path:
            - majordomus
            - worktree
            - inspect
          route: /docs/cli/worktree/inspect/
          executable: true
          usage: "majordomus worktree inspect [OPTIONS] <BRANCH>"
          about: "One branch: its canonical path, whether it exists, what occupies the path, the worktree holding it, and what stands in the way"
          args:
            - name: branch
              positional: true
              help: "The branch, full name"
              value_name: BRANCH
              takes_value: true
              required: true
              global: false
              possible_values: []
              defaults: []
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: worktree-inspect-branch
              title: "One branch, before creating its worktree"
              description: "Where the branch's worktree belongs, whether the branch exists, whether anything occupies the path, and what would stand in the way. For a branch that does not exist yet, the answer is the path `worktree create` would use and the command to run."
              argv:
                - worktree
                - inspect
                - feature/new-dashboard
              command: majordomus worktree inspect feature/new-dashboard
              setup: []
              expectation: "exits 0; prints -wt/feature/new-dashboard, does not exist yet"
          subcommands: []
        - path:
            - majordomus
            - worktree
            - create
          route: /docs/cli/worktree/create/
          executable: true
          usage: "majordomus worktree create [OPTIONS] [BRANCH]"
          about: "Create the canonical worktree of a branch, creating the branch from --base (default: the trunk) when it does not exist. The path is derived; none may be given"
          args:
            - name: branch
              positional: true
              help: "The branch, full name (`feature/improve-cli`)"
              value_name: BRANCH
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: base
              long: base
              positional: false
              help: "Start a new branch from this ref. Never fetched: it must resolve locally"
              value_name: REF
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: issue
              long: issue
              positional: false
              help: "Name the branch after this issue of .ai/repo/project/issues: `feature/<id>-<slug>`, the form the topology reads the issue back from"
              value_name: ID
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: worktree-create-branch
              title: Start work on a branch without deciding where it goes
              description: "Creates the branch `feature/improve-cli` from the trunk and checks it out in a new worktree at `<repository>-wt/feature/improve-cli`. No path is given and none may be: the destination follows from the repository's identity and the branch name, so the same command in the same repository always produces the same path — from the primary checkout, and from inside another worktree."
              argv:
                - worktree
                - create
                - feature/improve-cli
              command: majordomus worktree create feature/improve-cli
              setup: []
              expectation: "exits 0; prints -wt/feature/improve-cli, feature/improve-cli (new"
          subcommands: []
        - path:
            - majordomus
            - worktree
            - ensure
          route: /docs/cli/worktree/ensure/
          executable: true
          usage: "majordomus worktree ensure [OPTIONS] <BRANCH>"
          about: "The canonical worktree of a branch: created when absent, answered when present, refused when the branch is checked out somewhere else"
          args:
            - name: branch
              positional: true
              help: "The branch, full name"
              value_name: BRANCH
              takes_value: true
              required: true
              global: false
              possible_values: []
              defaults: []
            - name: base
              long: base
              positional: false
              help: "Start a new branch from this ref (default: the trunk)"
              value_name: REF
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: worktree-ensure-existing
              title: "The canonical worktree, whether or not it exists yet"
              description: "`ensure` is `create` for a caller that does not care whether the worktree is already there: it creates it when it is absent and answers the existing one when it is present. What a script or an agent runs before starting on a branch."
              argv:
                - worktree
                - ensure
                - feature/improve-cli
              command: majordomus worktree ensure feature/improve-cli
              setup:
                - argv:
                    - worktree
                    - create
                    - feature/improve-cli
                  command: majordomus worktree create feature/improve-cli
              expectation: "exits 0; prints exists, -wt/feature/improve-cli"
          subcommands: []
        - path:
            - majordomus
            - worktree
            - migrate
          route: /docs/cli/worktree/migrate/
          executable: true
          usage: "majordomus worktree migrate [OPTIONS]"
          about: "Bring every misplaced worktree to its canonical path, dirty state included, with a fingerprint taken before and after each move; --plan shows the steps and changes nothing"
          args:
            - name: plan
              long: plan
              positional: false
              help: Show the plan and change nothing
              value_name: PLAN
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: dry_run
              long: dry-run
              positional: false
              help: The same as --plan
              value_name: DRY_RUN
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: allow_copy
              long: allow-copy
              positional: false
              help: "When a move crosses filesystems, copy the tree, repair git's link, verify the copy against a manifest of every entry, and only then remove the original"
              value_name: ALLOW_COPY
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: only
              long: only
              positional: false
              help: Only these branches
              value_name: BRANCH
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: include_ephemeral
              long: include-ephemeral
              positional: false
              help: "Also move the scratch checkouts of sessions (under the temporary directory or .claude/worktrees), which are otherwise reported and left alone"
              value_name: INCLUDE_EPHEMERAL
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: worktree-migrate-plan
              title: What it would take to bring every worktree home
              description: "`--plan` shows each misplaced worktree with where it belongs, how it would move, the uncommitted work that moves with it, and what blocks it, and changes nothing. Without `--plan` the movable steps are carried out: each worktree is fingerprinted, moved with `git worktree move`, fingerprinted again at its new path, and reported as moved only when the two are equal."
              argv:
                - worktree
                - migrate
                - "--plan"
              command: majordomus worktree migrate --plan
              setup: []
              expectation: exits 0; prints nothing to migrate
          subcommands: []
        - path:
            - majordomus
            - worktree
            - validate
          route: /docs/cli/worktree/validate/
          executable: true
          usage: "majordomus worktree validate [OPTIONS]"
          about: "Every error of the topology, and nothing else; exit 10 when there is one"
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: worktree-validate-clean
              title: Is the topology valid?
              description: "Every error-level diagnostic and nothing else, then the verdict; exit 10 when there is an error. What a script gates on."
              argv:
                - worktree
                - validate
              command: majordomus worktree validate
              setup: []
              expectation: "exits 0; prints worktree topology: valid"
          subcommands: []
        - path:
            - majordomus
            - worktree
            - doctor
          route: /docs/cli/worktree/doctor/
          executable: true
          usage: "majordomus worktree doctor [OPTIONS]"
          about: "Every diagnostic of the topology, errors, warnings and facts, each with its code and remedy; exit 10 when there is an error"
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: worktree-doctor-clean
              title: "Every diagnostic, with its code and its remedy"
              description: "Errors, warnings and facts — a misplaced worktree, a stale registration, a detached HEAD, the primary checkout off the trunk, an unknown trunk — each under a stable code the API and the Cockpit carry too, each with the command that addresses it."
              argv:
                - worktree
                - doctor
              command: majordomus worktree doctor
              setup: []
              expectation: "exits 0; prints worktree topology: valid"
          subcommands: []
        - path:
            - majordomus
            - worktree
            - guard
          route: /docs/cli/worktree/guard/
          executable: true
          usage: "majordomus worktree guard [OPTIONS]"
          about: "May a mutation proceed from here? Exit 0 in a canonical worktree, in the primary checkout on the trunk, or detached; exit 10 with the reason otherwise. What the pre-commit hook asks"
          args:
            - name: quiet
              long: quiet
              short: q
              positional: false
              help: Print nothing on success
              value_name: QUIET
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: worktree-guard-ok
              title: May a commit proceed from here?
              description: "The pre-commit hook's question. Exit 0 in a branch's canonical worktree, in the primary checkout on the trunk, or on a detached HEAD; exit 10 with the diagnostic and the remedy when a feature branch is being worked on somewhere it does not belong. The hook stays one line; this is the logic."
              argv:
                - worktree
                - guard
              command: majordomus worktree guard
              setup: []
              expectation: "exits 0; prints worktree guard: ok"
          subcommands: []
        - path:
            - majordomus
            - worktree
            - repair
          route: /docs/cli/worktree/repair/
          executable: true
          usage: "majordomus worktree repair [OPTIONS]"
          about: "Drop git's registrations of worktrees whose directories are gone, and repair the administrative links of the ones that exist. Deletes no directory"
          args:
            - name: dry_run
              long: dry-run
              positional: false
              help: Report what would be dropped and change nothing
              value_name: DRY_RUN
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: worktree-repair-dry-run
              title: What git would forget
              description: "`repair` drops git's registrations of worktrees whose directories no longer exist and lets git repair the administrative links of the ones that do. It deletes no directory and touches no branch; `--dry-run` reports what it would drop and changes nothing."
              argv:
                - worktree
                - repair
                - "--dry-run"
              command: majordomus worktree repair --dry-run
              setup: []
              expectation: exits 0; prints nothing to prune
          subcommands: []
        - path:
            - majordomus
            - worktree
            - remove
          route: /docs/cli/worktree/remove/
          executable: true
          usage: "majordomus worktree remove [OPTIONS] <SELECTOR>"
          about: "Remove one linked worktree by branch or path. Never the primary checkout, never a branch, never uncommitted work without --force"
          args:
            - name: selector
              positional: true
              help: An exact branch name or an exact path
              value_name: SELECTOR
              takes_value: true
              required: true
              global: false
              possible_values: []
              defaults: []
            - name: force
              long: force
              positional: false
              help: Remove it even though it holds uncommitted work or is locked
              value_name: FORCE
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: worktree-remove-clean
              title: "Remove a worktree, and keep its branch"
              description: "Removes the worktree and nothing else. The branch it held still exists: worktree lifecycle and branch lifecycle are separate, and deleting a branch is a git command a person types deliberately. A worktree with uncommitted work is refused rather than removed."
              argv:
                - worktree
                - remove
                - feature/improve-cli
              command: majordomus worktree remove feature/improve-cli
              setup:
                - argv:
                    - worktree
                    - create
                    - feature/improve-cli
                  command: majordomus worktree create feature/improve-cli
              expectation: "exits 0; prints removed, feature/improve-cli still exists"
          subcommands: []
        - path:
            - majordomus
            - worktree
            - cleanup
          route: /docs/cli/worktree/cleanup/
          executable: true
          usage: "majordomus worktree cleanup [OPTIONS]"
          about: "The branches merged into the trunk whose worktree is clean or absent: what could be removed. Removes nothing"
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: worktree-cleanup-nothing
              title: "What could go, and what it would take"
              description: "Every branch merged into the trunk whose worktree is clean or absent, with the two commands that would remove the worktree and then the branch. Derived state only: nothing is deleted here, and a dirty or unmerged worktree is never listed."
              argv:
                - worktree
                - cleanup
              command: majordomus worktree cleanup
              setup: []
              expectation: exits 0; prints cleanup-eligible
          subcommands: []
        - path:
            - majordomus
            - worktree
            - branches
          route: /docs/cli/worktree/branches/
          executable: true
          usage: "majordomus worktree branches [OPTIONS]"
          about: "Every local branch, one per line, for a shell completion that wants the live set"
          args:
            - name: without_worktree
              long: without-worktree
              positional: false
              help: Only branches with no worktree
              value_name: WITHOUT_WORKTREE
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: worktree-branches-list
              title: "The live branch set, for completion"
              description: "Every local branch, one per line, nothing else. A shell completion for `worktree path`, `create` or `remove` reads this rather than a list kept anywhere, so a branch created a second ago completes."
              argv:
                - worktree
                - branches
              command: majordomus worktree branches
              setup: []
              expectation: exits 0
          subcommands: []
    - path:
        - majordomus
        - product
      route: /docs/cli/product/
      executable: true
      usage: "majordomus product [OPTIONS] [COMMAND]"
      about: "The product: what this repository's tool does for a person, as the features under the layer declare it, with every surface, count and moment derived; the matrix of features against interfaces; the providers; and the model's own validation"
      args:
        - name: repo
          long: repo
          positional: false
          help: "Start the search for the repository root here (default: the current directory)"
          value_name: PATH
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: discovery
          long: discovery
          positional: false
          help: How declarative files are enumerated
          value_name: DISCOVERY
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: vcs
              help: "Tracked files, through the version-control index (the layer's contract)"
            - name: filesystem
              help: A walk of the work tree with the same glob semantics; untracked files included
          defaults:
            - vcs
        - name: strict
          long: strict
          positional: false
          help: Refuse to proceed when any file of the layer carries an error diagnostic
          value_name: STRICT
          takes_value: false
          required: false
          global: true
          possible_values: []
          defaults:
            - "false"
        - name: share
          long: share
          positional: false
          help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
          value_name: DIR
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: format
          long: format
          positional: false
          help: Output shape
          value_name: FORMAT
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: text
              help: Lines for a person
            - name: json
              help: "One JSON document, deterministic"
          defaults:
            - text
        - name: featured
          long: featured
          positional: false
          help: Only the features the homepage shows
          value_name: FEATURED
          takes_value: false
          required: false
          global: true
          possible_values: []
          defaults:
            - "false"
        - name: all
          long: all
          positional: false
          help: "Include drafts and deprecated features, not only the stable ones"
          value_name: ALL
          takes_value: false
          required: false
          global: true
          possible_values: []
          defaults:
            - "false"
        - name: area
          long: area
          positional: false
          help: Only features serving this operational area of the why catalogue
          value_name: AREA
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: module
          long: module
          positional: false
          help: Only features made of this capability module
          value_name: MODULE
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: names_command
          long: names-command
          positional: false
          help: Only features made of this shell command
          value_name: NAMES_COMMAND
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: surface
          long: surface
          positional: false
          help: "Only features exposed through this surface: cli, api, mcp, cockpit or docs"
          value_name: SURFACE
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: query
          long: query
          short: q
          positional: false
          help: "Case-insensitive text over identities, titles, headlines, summaries, tags and bodies"
          value_name: QUERY
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
      examples:
        - id: product-default-list
          title: "What the product does, as the layer declares it"
          description: "`product` with nothing after it lists the features, because listing is what a person wants when they ask what the tool is for. Every column is derived: the surfaces a feature is exposed through come from the modules, commands and kinds it names, never from the file."
          argv:
            - product
          command: majordomus product
          setup: []
          expectation: "exits 0; prints SLUG, SURFACES, feature(s)"
      subcommands:
        - path:
            - majordomus
            - product
            - list
          route: /docs/cli/product/list/
          executable: true
          usage: "majordomus product list [OPTIONS]"
          about: "Every feature, narrowed by any filter, with the surfaces derived for each"
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
            - name: featured
              long: featured
              positional: false
              help: Only the features the homepage shows
              value_name: FEATURED
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: all
              long: all
              positional: false
              help: "Include drafts and deprecated features, not only the stable ones"
              value_name: ALL
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: area
              long: area
              positional: false
              help: Only features serving this operational area of the why catalogue
              value_name: AREA
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: module
              long: module
              positional: false
              help: Only features made of this capability module
              value_name: MODULE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: names_command
              long: names-command
              positional: false
              help: Only features made of this shell command
              value_name: NAMES_COMMAND
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: surface
              long: surface
              positional: false
              help: "Only features exposed through this surface: cli, api, mcp, cockpit or docs"
              value_name: SURFACE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: query
              long: query
              short: q
              positional: false
              help: "Case-insensitive text over identities, titles, headlines, summaries, tags and bodies"
              value_name: QUERY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: product-list
              title: "Every stable feature, in presentation order"
              description: "Drafts are excluded unless `--all` is given; `--featured` narrows to the features the homepage shows. The filters are the facets the model derives — an area, a module, a command, a surface — so a module added to the executable is a filter without anything being registered."
              argv:
                - product
                - list
              command: majordomus product list
              setup: []
              expectation: "exits 0; prints SLUG, fixture-feature"
            - id: product-list-json
              title: "The same, as the shape the API and MCP answer with"
              description: "One domain model behind every projection: this document is what `GET /api/v1/product/features` returns and what the `majordomus_features` tool answers, with the counts, the fingerprint and the surfaces of every feature."
              argv:
                - product
                - list
                - "--format"
                - json
              command: majordomus product list --format json
              setup: []
              expectation: "exits 0; prints one JSON document carrying /counts/features, /features/0/surfaces, /fingerprint"
          subcommands: []
        - path:
            - majordomus
            - product
            - show
          route: /docs/cli/product/show/
          executable: true
          usage: "majordomus product show [OPTIONS] <ID>"
          about: "One feature in full: what it is made of, resolved, and everything derived from that"
          args:
            - name: id
              positional: true
              help: "The feature's id, which is also its slug and its route"
              value_name: ID
              takes_value: true
              required: true
              global: false
              possible_values: []
              defaults: []
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
            - name: featured
              long: featured
              positional: false
              help: Only the features the homepage shows
              value_name: FEATURED
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: all
              long: all
              positional: false
              help: "Include drafts and deprecated features, not only the stable ones"
              value_name: ALL
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: area
              long: area
              positional: false
              help: Only features serving this operational area of the why catalogue
              value_name: AREA
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: module
              long: module
              positional: false
              help: Only features made of this capability module
              value_name: MODULE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: names_command
              long: names-command
              positional: false
              help: Only features made of this shell command
              value_name: NAMES_COMMAND
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: surface
              long: surface
              positional: false
              help: "Only features exposed through this surface: cli, api, mcp, cockpit or docs"
              value_name: SURFACE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: query
              long: query
              short: q
              positional: false
              help: "Case-insensitive text over identities, titles, headlines, summaries, tags and bodies"
              value_name: QUERY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: product-show
              title: "One feature, with everything derived from what it names"
              description: "The record as its file declares it, then what nobody authored: the capabilities of its modules with their tools and routes, the commands with their summaries, the objects of its kinds counted, the rules with their class, the documents, the decisions, the claims with their status, the moments it answers, and the interfaces all of that adds up to."
              argv:
                - product
                - show
                - fixture-feature
              command: majordomus product show fixture-feature
              setup: []
              expectation: "exits 0; prints fixture-feature, surfaces, derived"
          subcommands: []
        - path:
            - majordomus
            - product
            - matrix
          route: /docs/cli/product/matrix/
          executable: true
          usage: "majordomus product matrix [OPTIONS]"
          about: "Every feature against every interface, and every module, command and kind against the features that name it"
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
            - name: featured
              long: featured
              positional: false
              help: Only the features the homepage shows
              value_name: FEATURED
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: all
              long: all
              positional: false
              help: "Include drafts and deprecated features, not only the stable ones"
              value_name: ALL
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: area
              long: area
              positional: false
              help: Only features serving this operational area of the why catalogue
              value_name: AREA
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: module
              long: module
              positional: false
              help: Only features made of this capability module
              value_name: MODULE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: names_command
              long: names-command
              positional: false
              help: Only features made of this shell command
              value_name: NAMES_COMMAND
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: surface
              long: surface
              positional: false
              help: "Only features exposed through this surface: cli, api, mcp, cockpit or docs"
              value_name: SURFACE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: query
              long: query
              short: q
              positional: false
              help: "Case-insensitive text over identities, titles, headlines, summaries, tags and bodies"
              value_name: QUERY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: product-matrix
              title: "Every feature against every interface, and what no feature names"
              description: "One row per feature with a mark per surface, then every module of the executable, every public command and every kind of the layer with the features that name it. A row with no feature is a gap the product page cannot hide."
              argv:
                - product
                - matrix
              command: majordomus product matrix
              setup: []
              expectation: "exits 0; prints FEATURE, cli, MODULE"
          subcommands: []
        - path:
            - majordomus
            - product
            - providers
          route: /docs/cli/product/providers/
          executable: true
          usage: "majordomus product providers [OPTIONS]"
          about: "Every provider the tool has an adapter for, with what this repository does with it"
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
            - name: featured
              long: featured
              positional: false
              help: Only the features the homepage shows
              value_name: FEATURED
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: all
              long: all
              positional: false
              help: "Include drafts and deprecated features, not only the stable ones"
              value_name: ALL
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: area
              long: area
              positional: false
              help: Only features serving this operational area of the why catalogue
              value_name: AREA
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: module
              long: module
              positional: false
              help: Only features made of this capability module
              value_name: MODULE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: names_command
              long: names-command
              positional: false
              help: Only features made of this shell command
              value_name: NAMES_COMMAND
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: surface
              long: surface
              positional: false
              help: "Only features exposed through this surface: cli, api, mcp, cockpit or docs"
              value_name: SURFACE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: query
              long: query
              short: q
              positional: false
              help: "Case-insensitive text over identities, titles, headlines, summaries, tags and bodies"
              value_name: QUERY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: product-providers
              title: Every provider the tool has an adapter for
              description: "One line per template the distribution ships, with the bootstraps this repository's policy renders through it, the client configuration it carries for the shared MCP server, and the hooks the policy wires. The set is the templates; nothing here is a list of vendors."
              argv:
                - product
                - providers
              command: majordomus product providers
              setup: []
              expectation: "exits 0; prints PROVIDER, agents"
          subcommands: []
        - path:
            - majordomus
            - product
            - validate
          route: /docs/cli/product/validate/
          executable: true
          usage: "majordomus product validate [OPTIONS]"
          about: Every finding over the model; exit 10 when any is an error
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
            - name: featured
              long: featured
              positional: false
              help: Only the features the homepage shows
              value_name: FEATURED
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: all
              long: all
              positional: false
              help: "Include drafts and deprecated features, not only the stable ones"
              value_name: ALL
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: area
              long: area
              positional: false
              help: Only features serving this operational area of the why catalogue
              value_name: AREA
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: module
              long: module
              positional: false
              help: Only features made of this capability module
              value_name: MODULE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: names_command
              long: names-command
              positional: false
              help: Only features made of this shell command
              value_name: NAMES_COMMAND
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: surface
              long: surface
              positional: false
              help: "Only features exposed through this surface: cli, api, mcp, cockpit or docs"
              value_name: SURFACE
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: query
              long: query
              short: q
              positional: false
              help: "Case-insensitive text over identities, titles, headlines, summaries, tags and bodies"
              value_name: QUERY
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
          examples:
            - id: product-validate
              title: Check the model before anything projects it
              description: "A reference that resolves to nothing, with the nearest candidate; a duplicate identity; a file name that disagrees with its id; a draft that is featured; a stable feature under its floors; and every module, command or kind no feature names. Exit 10 on any error."
              argv:
                - product
                - validate
              command: majordomus product validate
              setup: []
              expectation: "exits 0; prints feature(s), valid"
          subcommands: []
    - path:
        - majordomus
        - release
      route: /docs/cli/release/
      executable: true
      usage: "majordomus release [OPTIONS] [COMMAND]"
      about: "What this project has shipped and what it would ship next: the changelog derived from the layer's own records, the version the two writers state, and the one command that raises both"
      args:
        - name: repo
          long: repo
          positional: false
          help: "Start the search for the repository root here (default: the current directory)"
          value_name: PATH
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: discovery
          long: discovery
          positional: false
          help: How declarative files are enumerated
          value_name: DISCOVERY
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: vcs
              help: "Tracked files, through the version-control index (the layer's contract)"
            - name: filesystem
              help: A walk of the work tree with the same glob semantics; untracked files included
          defaults:
            - vcs
        - name: strict
          long: strict
          positional: false
          help: Refuse to proceed when any file of the layer carries an error diagnostic
          value_name: STRICT
          takes_value: false
          required: false
          global: true
          possible_values: []
          defaults:
            - "false"
        - name: share
          long: share
          positional: false
          help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
          value_name: DIR
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: format
          long: format
          positional: false
          help: How to render the answer
          value_name: FORMAT
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: text
              help: Lines for a person
            - name: json
              help: "One JSON document, deterministic"
          defaults:
            - text
      examples:
        - id: release-changelog
          title: "What has shipped, and what has not"
          description: "`release` with nothing after it renders the changelog. Every line of it is derived — a section per release the layer records, its decisions the ADRs dated inside that release's window, its changes the conventional commits in its range — so there is no file anyone can forget to update."
          argv:
            - release
          command: majordomus release
          setup: []
          expectation: exits 0; prints Changelog
      subcommands:
        - path:
            - majordomus
            - release
            - changelog
          route: /docs/cli/release/changelog/
          executable: true
          usage: "majordomus release changelog [OPTIONS]"
          about: "The changelog, composed from the layer's release records, the decisions dated inside each release's window, and the conventional commits in its range"
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: How to render the answer
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: release-changelog-json
              title: The same document every other surface answers with
              description: "What `GET /api/v1/changelog` returns, what the MCP resource `majordomus://changelog` carries, and what `majordomus generate changelog` writes into the reference. One value, four renderings."
              argv:
                - release
                - changelog
                - "--format"
                - json
              command: majordomus release changelog --format json
              setup: []
              expectation: "exits 0; prints one JSON document carrying /schema, /current, /sections"
          subcommands: []
        - path:
            - majordomus
            - release
            - version
          route: /docs/cli/release/version/
          executable: true
          usage: "majordomus release version [OPTIONS]"
          about: "The version the two writers state, whether they agree, and the bump the commits since the last release imply"
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: How to render the answer
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: release-version
              title: "The version, and the one the commits imply"
              description: "The version is stated in two files for a reason the release script gives: an installed tree has no Cargo.toml and the crate is compiled before the shell tool exists, so neither can read the other at run time. This says what both state, whether they agree, and what the conventional commits since the last release imply the next one should be."
              argv:
                - release
                - version
                - "--format"
                - json
              command: majordomus release version --format json
              setup: []
              expectation: "exits 0; prints one JSON document carrying /declared, /agree, /bump"
          subcommands: []
        - path:
            - majordomus
            - release
            - bump
          route: /docs/cli/release/bump/
          executable: true
          usage: "majordomus release bump [OPTIONS]"
          about: "Raise the version in both places at once, to the bump the commits imply or to one you name"
          args:
            - name: level
              long: level
              positional: false
              help: Raise by this much instead of by what the commits imply
              value_name: LEVEL
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: exact
              long: exact
              positional: false
              help: "Set exactly this version, instead of raising the current one"
              value_name: VERSION
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: dry_run
              long: dry-run
              positional: false
              help: Say what would change and write nothing
              value_name: DRY_RUN
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: How to render the answer
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: release-bump-dry-run
              title: "Raising it, in both places, once"
              description: "The bump defaults to what the commits imply — a breaking change is major, a feature is minor, anything else is patch — and `--level` or `--exact` overrides that when a person means something the commits do not say. It writes both files and nothing else; `scripts/release-version --check` then proves the work of one writer rather than the memory of one person. A repository that declares no version — the example runs in one with no crate — cannot be raised, and says so with exit 12 rather than inventing a number to raise from."
              argv:
                - release
                - bump
                - "--dry-run"
              command: majordomus release bump --dry-run
              setup: []
              expectation: exits 12
          subcommands: []
    - path:
        - majordomus
        - quality
      route: /docs/cli/quality/
      executable: false
      usage: "majordomus quality <COMMAND>"
      about: "What this executable's own public surface is held to: documentation, executable examples, module coverage, and every command accounted for against the capability registry"
      args: []
      examples: []
      subcommands:
        - path:
            - majordomus
            - quality
            - report
          route: /docs/cli/quality/report/
          executable: true
          usage: "majordomus quality report [OPTIONS]"
          about: "Measure the crate and report every finding, with the rule it breaks and what to do about it"
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: false
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
            - name: code
              long: code
              positional: false
              help: "Only findings carrying this code, e.g. RUST_PUBLIC_MISSING_EXAMPLE"
              value_name: CODE
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: path
              long: path
              positional: false
              help: Only findings under this repository-relative path prefix
              value_name: PATH
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: summary
              long: summary
              positional: false
              help: Print the counts and leave the findings out
              value_name: SUMMARY
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: include_baselined
              long: include-baselined
              positional: false
              help: "Show the findings the baseline already accepts, which are left out by default"
              value_name: INCLUDE_BASELINED
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: write_baseline
              long: write-baseline
              positional: false
              help: "Record today's findings as the accepted baseline, so the debt can shrink and cannot grow"
              value_name: WRITE_BASELINE
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
          examples:
            - id: quality-summary
              title: "Where the crate's public surface stands"
              description: "The counts alone: how much of the exported surface is documented and exampled, how many modules something exercises, and how the canonical operations stand against the command line, HTTP, OpenAPI and MCP. Exits 10 when any finding stands outside the recorded baseline."
              argv:
                - quality
                - report
                - "--summary"
              command: majordomus quality report --summary
              setup: []
              expectation: exits 0
            - id: quality-code-json
              title: "One kind of finding, with the rule and the remedy"
              description: "Filtered to one violation code. Every finding carries the rule that requires it, where it is, why it matters and what to do — which is what lets a person and an agent act on the same report."
              argv:
                - quality
                - report
                - "--code"
                - RUST_MODULE_MISSING_EXAMPLE
                - "--format"
                - json
              command: majordomus quality report --code RUST_MODULE_MISSING_EXAMPLE --format json
              setup: []
              expectation: "exits 0; prints one JSON document carrying /measured, /passes, /report/schema"
          subcommands: []
    - path:
        - majordomus
        - run
      route: /docs/cli/run/
      executable: true
      usage: "majordomus run [OPTIONS] <CAPABILITY>"
      about: "Run a capability as an execution and follow it: its steps, its progress and its output as they happen"
      args:
        - name: repo
          long: repo
          positional: false
          help: "Start the search for the repository root here (default: the current directory)"
          value_name: PATH
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: discovery
          long: discovery
          positional: false
          help: How declarative files are enumerated
          value_name: DISCOVERY
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: vcs
              help: "Tracked files, through the version-control index (the layer's contract)"
            - name: filesystem
              help: A walk of the work tree with the same glob semantics; untracked files included
          defaults:
            - vcs
        - name: strict
          long: strict
          positional: false
          help: Refuse to proceed when any file of the layer carries an error diagnostic
          value_name: STRICT
          takes_value: false
          required: false
          global: true
          possible_values: []
          defaults:
            - "false"
        - name: share
          long: share
          positional: false
          help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
          value_name: DIR
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: capability
          positional: true
          help: "The capability to run, by its canonical id (`health.report`, `objects.verify`)"
          value_name: CAPABILITY
          takes_value: true
          required: true
          global: false
          possible_values: []
          defaults: []
        - name: input
          long: input
          positional: false
          help: "Its input, as one JSON object; the capability's input schema is what validates it"
          value_name: JSON
          takes_value: true
          required: false
          global: false
          possible_values: []
          defaults: []
        - name: follow
          long: follow
          positional: false
          help: Print the events as they arrive on stderr; on by default when stderr is a terminal
          value_name: FOLLOW
          takes_value: false
          required: false
          global: false
          possible_values: []
          defaults:
            - "false"
        - name: quiet
          long: quiet
          positional: false
          help: Print nothing but the final output
          value_name: QUIET
          takes_value: false
          required: false
          global: false
          possible_values: []
          defaults:
            - "false"
        - name: format
          long: format
          positional: false
          help: Output shape
          value_name: FORMAT
          takes_value: true
          required: false
          global: false
          possible_values:
            - name: text
              help: Lines for a person
            - name: json
              help: "One JSON document, deterministic"
          defaults:
            - text
      examples:
        - id: run-demonstrate
          title: Watch an execution happen
          description: "`run` starts a capability as an execution and follows it to its end: every step, every line it logs and every advance of its progress, as the handler reports them. `executions.demonstrate` exists to make that visible without waiting for real work — it reads nothing and writes nothing, and its only effect is the events it produces. The same execution, started from the Cockpit, streams the same events to a browser."
          argv:
            - run
            - executions.demonstrate
            - "--input"
            - "{\"steps\":2,\"delay_ms\":0}"
            - "--format"
            - json
          command: "majordomus run executions.demonstrate --input '{\"steps\":2,\"delay_ms\":0}' --format json"
          setup: []
          expectation: "exits 0; prints one JSON document carrying /state, /id, /output/steps, /steps/0/name"
      subcommands: []
    - path:
        - majordomus
        - executions
      route: /docs/cli/executions/
      executable: true
      usage: "majordomus executions [OPTIONS] [COMMAND]"
      about: "The executions of the server serving this repository: what has run, what is running, and what each one said"
      args:
        - name: repo
          long: repo
          positional: false
          help: "Start the search for the repository root here (default: the current directory)"
          value_name: PATH
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: discovery
          long: discovery
          positional: false
          help: How declarative files are enumerated
          value_name: DISCOVERY
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: vcs
              help: "Tracked files, through the version-control index (the layer's contract)"
            - name: filesystem
              help: A walk of the work tree with the same glob semantics; untracked files included
          defaults:
            - vcs
        - name: strict
          long: strict
          positional: false
          help: Refuse to proceed when any file of the layer carries an error diagnostic
          value_name: STRICT
          takes_value: false
          required: false
          global: true
          possible_values: []
          defaults:
            - "false"
        - name: share
          long: share
          positional: false
          help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
          value_name: DIR
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: format
          long: format
          positional: false
          help: Output shape
          value_name: FORMAT
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: text
              help: Lines for a person
            - name: json
              help: "One JSON document, deterministic"
          defaults:
            - text
      examples:
        - id: executions-default-list
          title: What has run
          description: "`executions` with nothing after it lists what the server serving this repository has run, newest first. In a checkout where no server is running it says so rather than pretending: an execution lives in the process that accepted it."
          argv:
            - executions
          command: majordomus executions
          setup: []
          expectation: exits 0; prints execution
      subcommands:
        - path:
            - majordomus
            - executions
            - list
          route: /docs/cli/executions/list/
          executable: true
          usage: "majordomus executions list [OPTIONS]"
          about: "Every execution the server remembers, newest first"
          args:
            - name: state
              long: state
              positional: false
              help: "Only executions in this state (queued, running, cancelling, succeeded, failed, cancelled)"
              value_name: STATE
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: capability
              long: capability
              positional: false
              help: Only executions of this capability
              value_name: CAPABILITY
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: executions-list-json
              title: "Every execution, as one document"
              description: "The same answer `GET /api/v1/executions`, the MCP tool `majordomus_executions` and the Cockpit's Executions page render, with the counts beside it: how many are remembered, how many are active, how many are waiting for a worker and how many live channels are following them."
              argv:
                - executions
                - list
                - "--format"
                - json
              command: majordomus executions list --format json
              setup: []
              expectation: "exits 0; prints one JSON document carrying /count, /active, /queued, /live_channels"
          subcommands: []
        - path:
            - majordomus
            - executions
            - show
          route: /docs/cli/executions/show/
          executable: true
          usage: "majordomus executions show [OPTIONS] <ID>"
          about: "One execution in full: its state, its steps, its diagnostics and what it produced"
          args:
            - name: id
              positional: true
              help: "The execution's id"
              value_name: ID
              takes_value: true
              required: true
              global: false
              possible_values: []
              defaults: []
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: executions-show-absent
              title: An execution that is not there
              description: "An execution lives in the process that accepted it and is remembered in bounded numbers, so asking for one nothing ran says so and exits with the missing-artifact code rather than inventing an empty answer. Against a running server, the same command prints that execution's state, its steps and what it produced."
              argv:
                - executions
                - show
                - x-20260101T120000Z-4c3b2a19
              command: majordomus executions show x-20260101T120000Z-4c3b2a19
              setup: []
              expectation: exits 12
          subcommands: []
        - path:
            - majordomus
            - executions
            - events
          route: /docs/cli/executions/events/
          executable: true
          usage: "majordomus executions events [OPTIONS] <ID>"
          about: "One execution's retained events, oldest first"
          args:
            - name: id
              positional: true
              help: "The execution's id"
              value_name: ID
              takes_value: true
              required: true
              global: false
              possible_values: []
              defaults: []
            - name: after
              long: after
              positional: false
              help: Only events after this sequence number
              value_name: AFTER
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: executions-events-absent
              title: The events of an execution that is not there
              description: "The retained events of one execution, oldest first, after a sequence number — what a reconnecting client reads before it opens the live channel. For an execution nothing ran, the same refusal as `show`."
              argv:
                - executions
                - events
                - x-20260101T120000Z-4c3b2a19
              command: majordomus executions events x-20260101T120000Z-4c3b2a19
              setup: []
              expectation: exits 12
          subcommands: []
        - path:
            - majordomus
            - executions
            - cancel
          route: /docs/cli/executions/cancel/
          executable: true
          usage: "majordomus executions cancel [OPTIONS] <ID>"
          about: Ask an execution to stop
          args:
            - name: id
              positional: true
              help: "The execution's id"
              value_name: ID
              takes_value: true
              required: true
              global: false
              possible_values: []
              defaults: []
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: executions-cancel-absent
              title: Asking an execution that is not there to stop
              description: "Cancellation is cooperative: the flag is set and a task stops when it next looks at it. There is nothing to set for an execution nothing ran, and the command says so rather than reporting a success it did not have."
              argv:
                - executions
                - cancel
                - x-20260101T120000Z-4c3b2a19
              command: majordomus executions cancel x-20260101T120000Z-4c3b2a19
              setup: []
              expectation: exits 12
          subcommands: []
        - path:
            - majordomus
            - executions
            - protocol
          route: /docs/cli/executions/protocol/
          executable: true
          usage: "majordomus executions protocol [OPTIONS]"
          about: "The live channel's contract: where it is, what it writes, and the schema of each message"
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: executions-protocol
              title: "The live channel's contract, from the types that implement it"
              description: "Where the WebSocket is, how a subscription and a reconnect are expressed, every message type, and the JSON Schema of each — derived from the Rust types, so a client validating against this is validating against the implementation. OpenAPI cannot describe a socket, which is why this is a capability and not a paragraph."
              argv:
                - executions
                - protocol
                - "--format"
                - json
              command: majordomus executions protocol --format json
              setup: []
              expectation: "exits 0; prints one JSON document carrying /protocol_version, /websocket, /event_types/0, /stream_types/0, /limits/max_events"
          subcommands: []
    - path:
        - majordomus
        - devcontext
      route: /docs/cli/devcontext/
      executable: false
      usage: "majordomus devcontext [OPTIONS] <COMMAND>"
      about: "The context a development session should be given, compiled from the repository: for an issue, a milestone, an intent or a set of paths, what is selected and why, what was left out and why, what collapsed into what, and the budget"
      args:
        - name: repo
          long: repo
          positional: false
          help: "Start the search for the repository root here (default: the current directory)"
          value_name: PATH
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: discovery
          long: discovery
          positional: false
          help: How declarative files are enumerated
          value_name: DISCOVERY
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: vcs
              help: "Tracked files, through the version-control index (the layer's contract)"
            - name: filesystem
              help: A walk of the work tree with the same glob semantics; untracked files included
          defaults:
            - vcs
        - name: strict
          long: strict
          positional: false
          help: Refuse to proceed when any file of the layer carries an error diagnostic
          value_name: STRICT
          takes_value: false
          required: false
          global: true
          possible_values: []
          defaults:
            - "false"
        - name: share
          long: share
          positional: false
          help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
          value_name: DIR
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: format
          long: format
          positional: false
          help: Output shape
          value_name: FORMAT
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: text
              help: Lines for a person
            - name: json
              help: "One JSON document, deterministic"
          defaults:
            - text
      examples: []
      subcommands:
        - path:
            - majordomus
            - devcontext
            - compile
          route: /docs/cli/devcontext/compile/
          executable: true
          usage: "majordomus devcontext compile [OPTIONS]"
          about: "Compile the context for a piece of work: every selected object with its provenance, the reason and the confidence, everything left out with the reason, what was deduplicated, and the per-tier budget; exit 10 when what may not be dropped already exceeds the budget"
          args:
            - name: issue
              long: issue
              positional: false
              help: "An issue id (`I0301`) or its canonical identifier"
              value_name: ISSUE
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: milestone
              long: milestone
              positional: false
              help: "A milestone id or slug, or its canonical identifier"
              value_name: MILESTONE
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: intent
              long: intent
              positional: false
              help: "What the session is trying to do, in words; the only input the compiler infers from"
              value_name: INTENT
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: paths
              long: path
              positional: false
              help: A repository-relative path the work touches; repeat for each
              value_name: PATHS
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: uris
              long: uri
              positional: false
              help: A canonical identifier to seed with directly; repeat for each
              value_name: URIS
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: budget_tokens
              long: budget-tokens
              positional: false
              help: The ceiling in estimated tokens
              value_name: BUDGET_TOKENS
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: max_depth
              long: max-depth
              positional: false
              help: How far from a seed the walk goes
              value_name: MAX_DEPTH
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: floor
              long: floor
              positional: false
              help: "Relevance below which an entry is reported rather than given, between 0 and 1"
              value_name: FLOOR
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: all_blocking_rules
              long: all-blocking-rules
              positional: false
              help: "Every blocking rule of the layer, not only the ones the work reaches"
              value_name: ALL_BLOCKING_RULES
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: devcontext-compile-issue
              title: The context a session working on one issue should be given
              description: "The issue is the seed. Its milestone follows along the `belongs_to` edge of the composed graph, the code and the cases under the scope it declares follow from the paths, and the policy and the scope are governance every session is held to. Every selected line names the selector that reached it and why; everything left out is listed with the reason."
              argv:
                - devcontext
                - compile
                - "--issue"
                - I0001
              command: majordomus devcontext compile --issue I0001
              setup: []
              expectation: "exits 0; prints SELECTED, majordomus://issue/I0001, EXCLUDED"
            - id: devcontext-compile-json
              title: "The same, as the structure every other surface answers with"
              description: "The canonical form: `GET /api/v1/devcontext` and the `majordomus_devcontext` tool return this document. Entries keep their canonical identifier, the index's provenance, every discovery path with its confidence, and the cost in estimated tokens; nothing is flattened to prose."
              argv:
                - devcontext
                - compile
                - "--issue"
                - I0001
                - "--format"
                - json
              command: majordomus devcontext compile --issue I0001 --format json
              setup: []
              expectation: "exits 0; prints one JSON document carrying /selected/0/uri, /selected/0/discovered_by/0/reason, /budget/limit_tokens, /fingerprint"
          subcommands: []
        - path:
            - majordomus
            - devcontext
            - explain
          route: /docs/cli/devcontext/explain/
          executable: true
          usage: "majordomus devcontext explain [OPTIONS] <URI>"
          about: Why one canonical identifier is or is not in the context a request compiles to
          args:
            - name: uri
              positional: true
              help: "The canonical identifier, `majordomus://<kind>/<identity>`"
              value_name: URI
              takes_value: true
              required: true
              global: false
              possible_values: []
              defaults: []
            - name: issue
              long: issue
              positional: false
              help: "An issue id (`I0301`) or its canonical identifier"
              value_name: ISSUE
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: milestone
              long: milestone
              positional: false
              help: "A milestone id or slug, or its canonical identifier"
              value_name: MILESTONE
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: intent
              long: intent
              positional: false
              help: "What the session is trying to do, in words; the only input the compiler infers from"
              value_name: INTENT
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: paths
              long: path
              positional: false
              help: A repository-relative path the work touches; repeat for each
              value_name: PATHS
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: uris
              long: uri
              positional: false
              help: A canonical identifier to seed with directly; repeat for each
              value_name: URIS
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: budget_tokens
              long: budget-tokens
              positional: false
              help: The ceiling in estimated tokens
              value_name: BUDGET_TOKENS
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: max_depth
              long: max-depth
              positional: false
              help: How far from a seed the walk goes
              value_name: MAX_DEPTH
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: floor
              long: floor
              positional: false
              help: "Relevance below which an entry is reported rather than given, between 0 and 1"
              value_name: FLOOR
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: all_blocking_rules
              long: all-blocking-rules
              positional: false
              help: "Every blocking rule of the layer, not only the ones the work reaches"
              value_name: ALL_BLOCKING_RULES
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: devcontext-explain-seed
              title: Why one thing is in the context
              description: "The identifier is judged under the same request `compile` takes: selected, excluded with the reason, folded into another identifier, held by the index and never reached, or unknown."
              argv:
                - devcontext
                - explain
                - majordomus://issue/I0001
                - "--issue"
                - I0001
              command: majordomus devcontext explain majordomus://issue/I0001 --issue I0001
              setup: []
              expectation: "exits 0; prints selected, majordomus://issue/I0001"
          subcommands: []
        - path:
            - majordomus
            - devcontext
            - policy
          route: /docs/cli/devcontext/policy/
          executable: true
          usage: "majordomus devcontext policy [OPTIONS]"
          about: "The compiler's own rules: the tiers, every edge of the composed graph and what is done with it, the selectors, the defaults"
          args:
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: devcontext-policy
              title: "The compiler's own rules"
              description: "The tiers in the order the budget spends in, every edge kind the composed graph declares with the weight it is followed by or the reason it is refused, and which selectors infer rather than read."
              argv:
                - devcontext
                - policy
              command: majordomus devcontext policy
              setup: []
              expectation: "exits 0; prints TIER, is_a, REFUSED"
          subcommands: []
    - path:
        - majordomus
        - evidence
      route: /docs/cli/evidence/
      executable: false
      usage: "majordomus evidence [OPTIONS] <COMMAND>"
      about: "What actually ran and what it proves: every claim of the matrix against the runs recorded for it, one claim's proof, one test's claims, and the recording of a run that happened"
      args:
        - name: repo
          long: repo
          positional: false
          help: "Start the search for the repository root here (default: the current directory)"
          value_name: PATH
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: discovery
          long: discovery
          positional: false
          help: How declarative files are enumerated
          value_name: DISCOVERY
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: vcs
              help: "Tracked files, through the version-control index (the layer's contract)"
            - name: filesystem
              help: A walk of the work tree with the same glob semantics; untracked files included
          defaults:
            - vcs
        - name: strict
          long: strict
          positional: false
          help: Refuse to proceed when any file of the layer carries an error diagnostic
          value_name: STRICT
          takes_value: false
          required: false
          global: true
          possible_values: []
          defaults:
            - "false"
        - name: share
          long: share
          positional: false
          help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
          value_name: DIR
          takes_value: true
          required: false
          global: true
          possible_values: []
          defaults: []
        - name: format
          long: format
          positional: false
          help: Output shape
          value_name: FORMAT
          takes_value: true
          required: false
          global: true
          possible_values:
            - name: text
              help: Lines for a person
            - name: json
              help: "One JSON document, deterministic"
          defaults:
            - text
      examples: []
      subcommands:
        - path:
            - majordomus
            - evidence
            - show
          route: /docs/cli/evidence/show/
          executable: true
          usage: "majordomus evidence show [OPTIONS]"
          about: Every claim against the evidence recorded for it
          args:
            - name: state
              long: state
              positional: false
              help: "Only claims in this proof state (proven, inputs_unchanged, stale, failing, not_run, unrunnable, no_test)"
              value_name: STATE
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: status
              long: status
              positional: false
              help: "Only claims declaring this status (guaranteed, advisory, planned, rejected)"
              value_name: STATUS
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: findings
              long: findings
              positional: false
              help: Only the claims whose declared status the evidence does not support
              value_name: FINDINGS
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: check
              long: check
              positional: false
              help: Exit 10 when a claim declares a guarantee the evidence does not support
              value_name: CHECK
              takes_value: false
              required: false
              global: false
              possible_values: []
              defaults:
                - "false"
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: evidence-show-json
              title: "The whole join, as one document"
              description: "The same answer `GET /api/v1/evidence` and the MCP tool `majordomus_evidence` return: every claim with its proof state, the sentence that explains how that state was derived, the execution behind it, the files that have changed since, and the command that produces the proof again. The tallies count the whole matrix even when the claims are filtered, so a narrowed answer never misreports how much of it was examined."
              argv:
                - evidence
                - show
                - "--format"
                - json
              command: majordomus evidence show --format json
              setup: []
              expectation: "exits 0; prints one JSON document carrying /claims, /totals, /ledger/path, /findings"
          subcommands: []
        - path:
            - majordomus
            - evidence
            - claim
          route: /docs/cli/evidence/claim/
          executable: true
          usage: "majordomus evidence claim [OPTIONS] <ID>"
          about: "One claim: its proof state, the execution behind it, and how to reproduce it"
          args:
            - name: id
              positional: true
              help: "The claim id, as docs/CLAIMS.yaml spells it"
              value_name: ID
              takes_value: true
              required: true
              global: false
              possible_values: []
              defaults: []
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: evidence-claim-absent
              title: A claim the matrix does not declare
              description: "A claim id nothing declares is a not-found rather than an empty answer. A typo that read as `this claim has no evidence` is the one answer this command must never give, because it is indistinguishable from the finding the whole subsystem exists to report."
              argv:
                - evidence
                - claim
                - no-such-claim-exists
              command: majordomus evidence claim no-such-claim-exists
              setup: []
              expectation: exits 13
          subcommands: []
        - path:
            - majordomus
            - evidence
            - proves
          route: /docs/cli/evidence/proves/
          executable: true
          usage: "majordomus evidence proves [OPTIONS] <ID>"
          about: "One test: its latest execution and every claim it proves"
          args:
            - name: id
              positional: true
              help: "`suite:<case>`, `crate:<binary>`, or the path a claim names it with"
              value_name: ID
              takes_value: true
              required: true
              global: false
              possible_values: []
              defaults: []
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: evidence-proves-unknown
              title: Something that names no test
              description: "A test is named by its identity (`suite:<case>`, `crate:<binary>`) or by the path a claim writes down, and the two resolve to the same thing. An argument that is neither is refused with the spellings it could have been, rather than answered with a test that proves nothing."
              argv:
                - evidence
                - proves
                - not-a-test
              command: majordomus evidence proves not-a-test
              setup: []
              expectation: exits 13
          subcommands: []
        - path:
            - majordomus
            - evidence
            - record
          route: /docs/cli/evidence/record/
          executable: true
          usage: "majordomus evidence record [OPTIONS]"
          about: Record a run that happened into the ledger
          args:
            - name: suite
              long: suite
              positional: false
              help: "The runner's TSV report (`MJ_TEST_REPORT=<file> bash test/run.sh`)"
              value_name: SUITE
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: crate_output
              long: crate-output
              positional: false
              help: "A file holding `cargo test`'s output, for the crate's own integration tests"
              value_name: CRATE_OUTPUT
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: origin
              long: origin
              positional: false
              help: "Where the run happened: local (the default), ci or release"
              value_name: ORIGIN
              takes_value: true
              required: false
              global: false
              possible_values: []
              defaults: []
            - name: repo
              long: repo
              positional: false
              help: "Start the search for the repository root here (default: the current directory)"
              value_name: PATH
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: discovery
              long: discovery
              positional: false
              help: How declarative files are enumerated
              value_name: DISCOVERY
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: vcs
                  help: "Tracked files, through the version-control index (the layer's contract)"
                - name: filesystem
                  help: A walk of the work tree with the same glob semantics; untracked files included
              defaults:
                - vcs
            - name: strict
              long: strict
              positional: false
              help: Refuse to proceed when any file of the layer carries an error diagnostic
              value_name: STRICT
              takes_value: false
              required: false
              global: true
              possible_values: []
              defaults:
                - "false"
            - name: share
              long: share
              positional: false
              help: "The tool distribution's share directory (kinds.yaml, schemas/); default: $MAJORDOMUS_SHARE, then the repository's own share/, then the one beside the executable"
              value_name: DIR
              takes_value: true
              required: false
              global: true
              possible_values: []
              defaults: []
            - name: format
              long: format
              positional: false
              help: Output shape
              value_name: FORMAT
              takes_value: true
              required: false
              global: true
              possible_values:
                - name: text
                  help: Lines for a person
                - name: json
                  help: "One JSON document, deterministic"
              defaults:
                - text
          examples:
            - id: evidence-record-missing
              title: Recording a report that is not there
              description: "The recorder reads what a run already wrote — the suite's TSV report, `cargo test`'s output — and stamps it with the provenance the run did not carry. A report it cannot read is refused: recording nothing would leave every claim reading `not run` after a run that ran, which is a lie in the safe direction and still a lie."
              argv:
                - evidence
                - record
                - "--suite"
                - target/no-such-run.tsv
              command: majordomus evidence record --suite target/no-such-run.tsv
              setup: []
              expectation: exits 13
          subcommands: []
