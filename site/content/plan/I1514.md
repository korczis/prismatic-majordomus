+++
title = "I1514 — The API reference is pages a browser audit can read"
description = "Publish the HTTP API reference as an index, a page per tag and a page of shared types, so that no route carries the whole document and the accessibility audit can finish on every one of them."
weight = 237
[extra]
plan_id = "I1514"
source = ".ai/repo/project/issues/I1514.yaml"
+++
