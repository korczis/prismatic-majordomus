+++
title = "majordomus worktree status"
description = "Where this call is — branch, worktree, canonical or not, uncommitted work — and how many errors the whole topology carries; exit 10 when this worktree is out of place"
weight = 49
[extra]
route = "/docs/cli/worktree/status/"
command = "majordomus worktree status"
source = "apps/majordomus-cli/src/cli.rs"
+++
