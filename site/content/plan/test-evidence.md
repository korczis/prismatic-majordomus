+++
title = "test-evidence — Every verifiable claim and widget resolves to revision-bound test evidence, on every surface"
description = "Evidence is indexed by subject and judged at the presented revision, as ADR 0087 records. Every claim, rule, feature, command, capability, MCP tool and use case is a subject of one derived index, and every widget that states behaviour names one. Every surface reads its verdict from the tracked ledger alone, through one freshness function and one aggregation, at the revision it presents: the CLI and HTTP and MCP at the working tree or the checked-out commit, the site at the commit it was built from. A trunk run reaches the ledger through a reviewed recording pull request, and a supplementary record (a run record, coverage, a local row) can only weaken a verdict, naming itself when it does. Verified is rendered only for the one proven row of the freshness table, on every surface."
weight = 21
template = "milestone.html"
[extra]
plan_id = "test-evidence"
source = ".ai/repo/project/milestones/test-evidence.yaml"
+++
