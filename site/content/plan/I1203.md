+++
title = "I1203 — One flow serves every provider, and it refuses everything it did not start"
description = "Implement the authorisation-code flow with PKCE once, over the typed provider model: a start route that issues state and nonce and redirects to the provider's authorisation endpoint, a callback route that refuses a state it did not issue or that has expired, exchanges the code, verifies the issuer, the audience, the expiry and the nonce of the returned identity token, and resolves the stable subject from the claim the object names."
weight = 199
[extra]
plan_id = "I1203"
source = ".ai/repo/project/issues/I1203.yaml"
+++
