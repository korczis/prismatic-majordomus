# The backlog of a projection that stopped being applied, as a ceiling.
#
# Written by scripts/ci/github-check --write-baseline; never by hand. These two
# states are the records the projection has not reached yet, and the number may
# only fall. Every other state is refused outright and has no line here.
missing 192
adopt 19
