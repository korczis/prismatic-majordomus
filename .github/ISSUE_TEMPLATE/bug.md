---
name: Bug
about: A documented behaviour that is not real, or a command that misbehaves
---

**What did you run?** (exact command)

**What happened?** (exact output, including the exit code: `echo $?`)

**What did the documentation say would happen?** (link to the line in `docs/CLI.md` or `README.md`)

**Environment:** `bash --version | head -1`, `git --version`, OS
